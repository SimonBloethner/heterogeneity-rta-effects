\documentclass[12pt,a4paper]{article}

% Packages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{natbib}
\usepackage{hyperref}
\usepackage{geometry}
\usepackage{setspace}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}

% Page geometry
\geometry{margin=1in}

% Line spacing
\onehalfspacing

% Math operators
\newcommand{\E}{\mathbb{E}}
\DeclareMathOperator{\Var}{Var}
\DeclareMathOperator{\Cov}{Cov}
\DeclareMathOperator{\Corr}{Corr}
\DeclareMathOperator{\SD}{SD}

% Theorem environments
\newtheorem{assumption}{Assumption}
\newtheorem{proposition}{Proposition}
\newtheorem{corollary}{Corollary}
\newtheorem*{remark}{Remark}

% Proposition constants (auto-generated by S26_prop_verification.R)
\input{prop_constants}
\input{a2_constants}
\input{ledger_constants}

% Title information
\title{Heterogeneity in RTA Trade Effects}
\author{Simon Bl\"othner\thanks{Austrian Institute of Economics and Social Philosophy, Operngasse 6/1, 1010, Vienna, Austria; University of Bayreuth, Universit\"atsstraße 30, 95447, Bayreuth, Germany. Email: \texttt{s.bloethner@austrian-institute.org}}}
\date{\today}

\begin{document}

\maketitle

\begin{abstract}
Estimates of the effect of regional trade agreements are reported as single
coefficients, on the implicit assumption that agreements act alike on the pairs
that sign them. We estimate the distribution of those effects instead, and show
first that the diagnostics which would ordinarily license pair-level estimation
cannot do so: split-half reliability on treated pairs is largely reproduced on a
placebo population with no effects to recover, so what such statistics certify
is persistence rather than validity. Correcting the identified biases in
sequence and deconvolving the residual estimation noise, the standard deviation
of true effects lies in $[\LedgerSDTrueLo,\,\LedgerSDTrueHi]$, an identified set
% [SD_THETA_TRUE]
whose endpoints differ in a noise-attribution assumption these data cannot
settle, and the probability that a randomly drawn adopting pair gains nothing
lies in $[\LedgerPLo,\,\LedgerPHi]$. Effects fall steeply in pre-adoption trade
% [P_THETA_LEQ_0]
and then floor; a conditional general-equilibrium solve finds transmission flat
across the size distribution, so the gradient in outcomes is a gradient in the
effect parameter rather than an artifact of scale. The shape of the distribution
is not identified at this signal share, and no larger sample relieves the limit.
We state the diagnostic practices that survive.
\end{abstract}

\textbf{Keywords:} regional trade agreements; gravity model; treatment
effect heterogeneity; partial identification; split-half reliability

\textbf{JEL Codes:} F14, F15, C23, C14

\newpage

\section{Introduction}
\label{sec:introduction}
% --------------------------------------------------------------------------- % P1 — The question mismatch; pooling destroys the contingency structure. % ---------------------------------------------------------------------------

A government weighing a trade agreement asks a question about itself: what will this agreement do to our trade? The empirical literature answers a different question: what have such agreements done, on average, across all pairs of countries that ever signed one. The two questions have the same answer only if agreements affect all pairs alike. If they do not, the average describes the treaty stock rather than any member of it, and the averaging itself does damage beyond answering the wrong question: it pools entities that appear related but differ qualitatively — in what they trade, in how their economies interlock, in what the agreement can reach — and smooths over the distinct causal stories that generated the data \citep{heckman2001micro}. What the questioner wanted was precisely the contingency the pooling removes. Whether this substitution of one question for another is harmless is an empirical matter. This paper argues that in the flagship empirical model of trade it is not, and that its cost has been systematically underestimated because the tools that should reveal it are structurally unable to.

% --------------------------------------------------------------------------- % P2 — Heterogeneity as mediation of the known by the unknown; homogeneity is % the strong assumption. % ---------------------------------------------------------------------------

There are good reasons to expect the substitution to matter. An agreement changes the terms under which particular exporters in one economy meet particular buyers in another, and what follows depends on the complementarity of the two economies, the capacity behind their existing trade, and the quality of the match the agreement deepens — determinants no dataset records and no regressor represents. When relevant determinants go unobserved, the influence of the observed ones does not survive untouched: the response to a measured policy is mediated by, and contingent on, the unmeasured conditions under which it operates. Heterogeneity in treatment effects is the statistical name for this contingency — the form that dependence on the unmeasured takes when viewed from inside a regression, represented as variation in the effect precisely because it cannot be represented as a covariate \citep{pesaran1995estimating}. Homogeneity, by contrast, is the assertion that none of these conditions matter: that the same treaty raises trade between neighboring industrial economies and between distant, barely connected ones by an identical proportion. Framed this way, the common-effect specification is not a neutral default awaiting evidence against it. It is the strong assumption, and the burden it has quietly carried in applied work deserves examination rather than presumption.

% --------------------------------------------------------------------------- % P3 — The double-nested nonlinearity: the coefficient is an input to a % nonlinear model, whose output is an input to a nonlinear payoff. % ---------------------------------------------------------------------------

The substitution would still be tolerable if the estimated coefficient were consumed as a number — reported, compared, filed. It is not. It is consumed as an input, and the chain it enters is nonlinear at every link. The agreement coefficient enters trade-cost functions exponentially, and through them the counterfactual general-equilibrium systems in which the gravity literature conducts its policy analysis, where prices, incomes, and multilateral resistances adjust jointly. Nonlinear transformations do not commute with averaging \citep{jensen1906fonctions}: the outcome implied by the average effect is not the average of the outcomes implied by the effects, and under convexity, dispersion is not noise surrounding the answer but part of the answer, shifting levels rather than merely widening bands. Nor does the chain end at the model's output. The users of these counterfactuals do not care about a trade elasticity, or even a trade response, for their own sake; they care about what the response does to the things the analysis is meant to inform — revenues, adjustment burdens, the position of an economy vis-à-vis its partners — and that second mapping, from model output to consequence, is nonlinear once more. Two nested nonlinearities amplify whatever the point estimate conceals. Small differences in the estimate, and modest amounts of hidden dispersion around it, compound into large differences in the quantities the exercise exists to compute. Put plainly, a point estimate fed into a nonlinear model is an implicit distributional assumption — it asserts that the distribution of effects is degenerate at a point — and if the true distribution is dispersed, skewed, or tilted across the very pairs the counterfactual concerns, the model returns confident answers about a world that does not exist, with the confidence inherited from an assumption nobody stated.

% --------------------------------------------------------------------------- % P4 — "The average" is equivocal upstream: estimators are weightings; under % heterogeneity defensible specifications must disagree. % ---------------------------------------------------------------------------

Nor does the difficulty begin only where the estimate is used; it begins where the estimate is made. "The average effect" reads as a single object, but estimators do not compute unweighted means over pairs. Every fixed-effects structure weights the pairs by the variation left after its absorptions, and different structures — absorbing time, absorbing pair, absorbing country-by-time — leave different variation and therefore weight different pairs. Under homogeneity this is immaterial: all weightings of a constant agree, and the well-documented sensitivity of gravity estimates to specification can only be read as fragility, an embarrassment to be managed. Under heterogeneity the same sensitivity is a prediction. If effects vary systematically along dimensions the weights track, then correctly specified, individually defensible estimators must disagree, and the pattern of their disagreement is information about the shape of what is being averaged. Misread through the common-effect lens, that information becomes something worse than wasted: it becomes an invitation to choose among specifications by their conclusions, with each choice defensible and none interpretable.

% --------------------------------------------------------------------------- % P5 — The escape route fails silently: diagnostics certify persistence, not % truth. % ---------------------------------------------------------------------------

The natural escape is to stop averaging: estimate the effect pair by pair and study the collection. But this route fails in a way that is more instructive than the failure of the average, because it fails while passing every test. The instruments used to police unit-level estimates — replication across subsamples, stability across time, the reliability of cross-sectional rankings — all measure persistence. They ask whether an estimate returns when we look again. Yet the dominant distortions in pair-level trade data are themselves persistent characteristics of pairs: biases rooted in volatility and drift do not wash out on a second look; they reappear, and a diagnostic that certifies whatever persists will certify them with the same authority it would certify the truth. Reliable and wrong is a possible state of an estimate, and it is a state the standard toolkit cannot detect from the inside. Confidence, if it is to be had, must be earned differently: not by observing that estimates replicate, but by identifying the mechanisms that would make artifacts replicate, and removing them.

% --------------------------------------------------------------------------- % P6 — Stakes assembled; the distribution is the object; downside asymmetry % names the decision-relevant deliverables (seeds the discussion). % ---------------------------------------------------------------------------

Assembled, the exposures compound. Whoever relies on an average agreement effect is exposed three times over: the average answers a question about the treaty stock rather than the question asked; the nonlinear chain it feeds amplifies whatever it conceals; and the diagnostics that should sound the alarm are structurally silent. None of this is visible in the point estimate or its standard error, which may be small, stable, and beside the point. What would resolve all three exposures is the same object: the distribution of agreement effects across pairs — its center, its spread, its skew, and its variation across the kinds of pairs that policy distinguishes. The resolution is more than formal. Decisions made under uncertainty are rarely symmetric in their stakes, and what a decision-maker needs from an effect estimate — how bad the bad case is, how likely it is, and how good the good case gets — is a property of the distribution's shape rather than of its mean, however precisely that mean is estimated. Whether a shape at that resolution can be recovered from data of this kind is not obvious in advance, and it is one of the questions this paper settles. With the distribution in hand, the question a government asks has an answer in the form it was posed; the nonlinear model can be fed what it actually requires; and the disagreement among specifications becomes decomposable rather than embarrassing. One qualification travels with all three: such a distribution is estimated over the pairs that chose to sign, and Section~\ref{sec:limits-transport} shows that this is a property of the design rather than a caution about it.

% --------------------------------------------------------------------------- % P7 — Why the distribution should have structure: multiplicativity as the % default character of economic change (grounds the mechanism section). % ---------------------------------------------------------------------------

There is, moreover, a reason to expect this distribution to have structure rather than being an amorphous cloud of noise. Economic magnitudes evolve by compounding: growth arrives as relative change applied to an existing state, so that trade between two economies at any moment is the residue of multiplied increments rather than summed ones \citep{gibrat1931, sutton1997gibrat, Jones2016, Beare2022}. A policy acting on such a process plausibly acts the same way — scaling what is there rather than adding to it — and multiplicative action by unmeasured conditions carries testable signatures: lognormal outcomes, asymmetry in effects, effects graded in the size of what they act upon, and selection of agreements on the very conditions that shape them \citep{mitzenmacher2004brief}. These signatures are the bridge between the motivation above and the evidence below: they are what the data would display if the contingency argued for in this introduction is real. How many of the four a design of this kind can actually adjudicate is a further question, and one the paper reports on rather than assumes away.

% --------------------------------------------------------------------------- % P8 — Contributions, one qualitative paragraph. % ---------------------------------------------------------------------------

This paper estimates the distribution of agreement effects for the flagship empirical model of trade, and makes four contributions along the way. First, we derive the failure of the diagnostics formally: we show why naive pair-level estimation produces estimates that are simultaneously badly biased and nearly perfectly reliable, and we state the conditions under which no persistence-based diagnostic can distinguish artifact from effect. Second, we construct the corrected distribution, removing the identified biases in sequence and the remaining estimation noise by deconvolution; where the data are genuinely silent — on the boundary between transitory effect variation and treatment-coincident volatility — we report a set rather than an assumption-dependent point, which is the same honesty applied at the second moment that motivates the paper at the first. Third, we resolve the disagreement among specifications constructively, showing that the estimates standard structures produce are differently weighted averages of the one distribution we recover, so that the specification spread is explained rather than adjudicated. Fourth, we propagate the distribution through general equilibrium and report what survives transmission and under what conditions: the size gradient survives, so that variation in outcomes across pairs is variation in the effect parameter rather than an artifact of scale, while every propagated range remains indexed by the dispersion arm it was computed under and by a distributional form imposed where the shape is not identified. Whoever quotes one of these numbers inherits both conditions, and we report them as part of the result rather than as caveats appended to it.

% --------------------------------------------------------------------------- % P9 — Roadmap (compressed). % ---------------------------------------------------------------------------

Section~\ref{sec:theory} develops the mechanism and states the four predictions
it commits to. Section~\ref{sec:data} describes the data and the construction of
the estimating population. Section~\ref{sec:empirical} presents the naive
estimator and its diagnostics, derives the reliability results as formal
propositions, and sets out the corrections. Section~\ref{sec:results} reports the
corrected distribution, its variation across pair types, and the reconciliation
of the specification spread. Section~\ref{sec:limits} establishes the boundary of
what these data can identify, and shows that part of that boundary does not move
with sample size.
Section~\ref{sec:propagation} propagates the distribution through general
equilibrium. Section~\ref{sec:conclusion} concludes and reports which of the four
predictions survived. Proofs are collected in Appendix~\ref{app:proofs}.


\section{\label{sec:theory}Theoretical Framework}

\subsection{\label{sec:theory-primitive}The Economic Primitive}

% ¶1 — Hands off from the roadmap: fixes what structural gravity supplies and
% names the residual pair-specific variation as this section's object.

Structural gravity supplies the accounting within which bilateral trade is
organized: an exporter's supply capacity, an importer's expenditure, and the
trade costs between them measured relative to the costs each faces with every
other partner, all disciplined by general-equilibrium consistency
\citep{anderson2003gravity, HeadMayer2014}. That
apparatus fixes the size and distance terms and, in the fixed-effects form now
standard, absorbs whatever varies by exporter-year and by importer-year. What it
does not fix is the pair. Two dyads matched on exporter capacity, importer
expenditure and distance routinely trade at volumes that differ by more than the
residual variation the model treats as incidental, and the systematic part of
that difference is the object of this section. The question is not whether such
variation exists but what kind of object generates it, because the answer
determines the shape of what a policy acting on such pairs can produce.

% ¶2 — States the primitive: pair conditions are unobserved, non-substitutable,
% and therefore compound rather than add.

Trade between two economies requires that a series of conditions hold at once.
What one economy produces must be what the other can use, at the stage of
processing at which it is offered; contracts written under one legal order must
be enforceable, in practice and not only on paper, under the other; the pair
must sit on shipping, payment and correspondent-banking networks that already
carry traffic between them; and the administrative apparatus of customs
classification, standards recognition and documentation must be in place to a
depth sufficient for goods to move without prohibitive delay. These conditions
are specific to the pair, are individually unobserved, and no dataset records
them. Decisively, they do not substitute for one another
\citep{kremer1993oring}. A shipment occurs because every link in the chain
holds; an unusually complementary production structure does not compensate for
an unenforceable contract, and a well-functioning payments corridor does not
compensate for goods the importer cannot use. Conditions of this kind enter the
outcome multiplicatively rather than additively, and the compounding is not a
modeling convenience but a restatement of what it means for a chain to hold.
Multiplicativity is itself a consequence of heterogeneity under complementarity:
when the value of an activity depends on which others are present, the
combinations available expand as a function of those already realized, and
growth that builds on the existing stock in this way is multiplicative by
construction.

% ¶3 — Establishes that multiplicative structure is the discipline's default,
% not an exotic assumption imported for convenience.

This structure is neither peculiar to trade nor unfamiliar. It is the production
structure of the weak-link literature, in which output under complementarity is
governed by the least functional element rather than by the average of the
elements, so that a defect at any point in a chain reduces the value of
competence everywhere else \citep{kremer1993oring, jones2011weaklinks}. The same
logic organizes the older capital-theoretic treatment of complementarity, in
which the productivity of an addition to the capital stock depends on the
composition of the stock it joins rather than on its own quantity
\citep{von1921positive, hayek1941, lachmann1956}. More broadly, economic
processes are characterized by mechanisms that reinforce what is already in
place: network effects and increasing returns that compound over time
\citep{pietronero2001explaining, gabaix2009power}; agglomeration
\citep{krugman1991increasing}; and learning-by-doing, in which capability is a
function of accumulated activity \citep{arrow1962economic, romer1986increasing}.
In each case the marginal contribution of a factor is conditioned on the
configuration of the others rather than being fixed. Taking the bilateral pair
as the unit at which such configurations are realized, the residual variation
left by structural gravity is what a multiplicative interaction of unobserved
pair conditions would look like when viewed through a specification that has no
place to put them.

\subsection{\label{sec:theory-distribution}From Primitive to Distribution}

% ¶4 — Derives prediction (i) from the primitive, and immediately discounts its
% evidentiary weight; sets up why the paper needs a conjunction.

If bilateral outcomes are the product of many pair-specific factors that vary
independently enough not to move as one, the logarithm of the outcome is a sum
of many varying components, and the distribution of outcomes across pairs
belongs to the lognormal family --- Gibrat's law of proportionate effect applied
to the dyad rather than the firm \citep{gibrat1931, sutton1997gibrat}. This is
the mechanism's first prediction: (i) bilateral trade flows are distributed
lognormally across pairs, with the right-tail weight that family implies. The
prediction is genuine, in that a mechanism generating additively separable
conditions would not produce it, but its discriminating power is limited and it
would be dishonest to present it otherwise. Lognormal outcomes are
overdetermined: random proportionate growth, the aggregation of heterogeneous
sub-units, and several other generative stories produce the same signature, and
distinguishing among them from the shape of a single cross-section is generally
not possible \citep{mitzenmacher2004brief}. Confirming (i) therefore removes
rival mechanisms that predict something else while leaving standing every rival
that predicts the same thing, which is why this paper does not rest its case on
this signature, and why the mechanism must be made to commit to predictions
about the treatment effect and not only about the outcome.

\subsection{\label{sec:theory-nonsep}Non-Separable Treatment Effects}

% ¶5 — Locates where the agreement enters, and contrasts the two error
% structures available to it; the legacy specification argument, inline.

The empirical models used to estimate trade flows are themselves multiplicative.
The workhorse specification writes the flow as $y_{ij} =
\exp(\mathbf{x}_{ij}'\boldsymbol{\beta} + \epsilon_{ij})$, estimated by Poisson
pseudo-maximum likelihood under $\mathbb{E}[e^{\epsilon_{ij}} \mid
\mathbf{x}_{ij}] = 1$ \citep{silva2006log}. The functional form is
multiplicative but the shock is not: $\epsilon_{ij}$ enters in parallel to the
covariates, so that whatever the pair's unobserved conditions do to its trade,
they do independently of the policy variable sitting in $\mathbf{x}_{ij}$. An
agreement then shifts every pair's trade by the same proportion, and the pair
conditions of Section~\ref{sec:theory-primitive} are confined to setting the
level from which that common proportion is taken. This is a substantive
restriction, not a normalization. It asserts that the conditions determining
whether the chain holds have no bearing on what happens when one link in the
chain is loosened.

% ¶6 — States the alternative and its two readings; carries the section's one
% display equation and defines the effect as a modulated object.

The alternative is to let the covariates interact with the shock, $y_{ij} =
\exp(\mathbf{x}_{ij}'(\boldsymbol{\beta} + \boldsymbol{\epsilon}_{ij}))$, under
the corresponding normalization
$\mathbb{E}[e^{\mathbf{x}_{ij}'\boldsymbol{\epsilon}_{ij}} \mid \mathbf{x}_{ij}]
= 1$, which preserves the familiar conditional mean $\mathbb{E}[y_{ij}\mid
\mathbf{x}_{ij}] = e^{\mathbf{x}_{ij}'\boldsymbol{\beta}}$ and which we maintain
throughout. This form has two readings. Structurally, it is a random-coefficients
model in which the effect of each covariate, the agreement among them, varies
across bilateral pairs, with the variation governed by the distribution of
$\boldsymbol{\epsilon}_{ij}$. Empirically, it generates heteroskedasticity of the
kind long documented in trade data \citep{silva2006log}: pairs facing higher
systematic trade costs are more exposed to idiosyncratic frictions in
implementation, and therefore deviate more widely, in absolute terms, from what
the model predicts for them. Written in terms of the object of interest, the
agreement effect for a pair is
%
\begin{equation}
\theta_{ij} \;=\; f\!\left(A_{ij},\, c_{ij}\right),
\label{eq:nonsep}
\end{equation}
%
where $A_{ij}$ denotes the agreement and $c_{ij}$ the pair's unobserved
conditions --- rather than a scalar $\theta$ common to all pairs.
% [OPTIONAL — the following two sentences may be cut without consequence.]
Neither argument of $f$ is homogeneous. Agreements enter the empirical
literature as a binary indicator, but the legal instruments so indicated range
from tariff schedules with little else attached to architectures governing
services, procurement, standards and dispute settlement, and treating these as
one object imposes a second homogeneity assumption alongside the first; we do not
attempt to separate the two sources of dispersion here, and note only that
both are suppressed by the same specification and neither is consistent with a
scalar $\theta$.
% [END OPTIONAL]
The economics of the modulation is the economics of a chain. An agreement does
not create trade directly; it relaxes a constraint, lowering a tariff,
recognizing a standard, binding a dispute procedure. Whether relaxing that
constraint moves anything depends on whether the other links were holding, which
is precisely what $c_{ij}$ records. The effect is therefore modulated by the
pair's conditions rather than added to them, and three further predictions
follow from that modulation.

% ¶7 — Prediction (ii): right skew with a bounded floor, from the chain logic.

The first concerns shape. If the effect is the product of the relaxation and the
conditions it acts through, the effect distribution inherits the compounding of
the conditions and is therefore right-skewed rather than symmetric: (ii) most
pairs gain modestly, a minority whose conditions are aligned gain
multiplicatively, and the left tail is thin. The asymmetry has a specific
source, and it is worth stating plainly because it is what distinguishes this
prediction from a generic appeal to skewness. The upside is unbounded, in the
sense that the conditions can reinforce one another without limit; the downside
is bounded, because an agreement that fails to bind is inert rather than
destructive. A pair whose chain was broken before the agreement, and remains
broken after it, records no effect --- it does not record a loss. There are
channels through which an agreement could reduce a particular pair's trade,
diversion toward a third partner most obviously, but these operate against the
floor rather than mirroring the upside, so the mechanism predicts an effect
distribution with a short left tail and a long right one.

% ¶8 — Prediction (iii): effects graded in observables, which makes the
% mechanism falsifiable against something measurable.

The second concerns gradation, and it is the prediction that gives the mechanism
purchase on observable data. If an agreement works by relaxing constraints, its
effect is largest where the constraints bind hardest. But the constraints a
treaty addresses are discrete rather than continuous: a tariff line, a standards
recognition, a documentation requirement. Each either still obstructs the pair
or has already been surmounted, and a pair trading at volume through established
channels has, by revealed behavior, already surmounted them --- the fixed costs
of compliance are sunk, the customs relationships exist, and the trade would not
be occurring otherwise. Relaxing what has already been worked around changes
little. The prediction is therefore that (iii) effects are graded in pair size
and prior integration, but not smoothly: they are large where the treaty's
constraints still bind and fall to a common floor once they do not. What the
mechanism forbids is continued decline among pairs that have already exhausted
the margin, since beyond exhaustion there is nothing left to grade. This is
sharper than (i) and (ii), because it names both a direction and a shape against
variables the data contain, and it is falsified not only by a positive slope but
by a smoothly monotone negative one.

% ¶9 — Prediction (iv): selection on the same unobservables, framed as
% assortative matching rather than as a caveat about identification.

The third concerns who signs. The conditions in $c_{ij}$ are not observed by the
econometrician, but they are observed, imperfectly and in the aggregate, by the
governments and the exporters who lobby them. Agreements are negotiated where
there is something to negotiate over, which is to say where enough of the chain
already holds that the parties can foresee gains from relaxing what remains. The
prediction is that (iv) agreements are selected on the same unobserved conditions
that modulate their effects: treated pairs are drawn from the stable end of the
condition distribution rather than at random from it. This is assortative
matching of the kind that complementarity produces generally, where partners of
similar reliability are matched together in equilibrium precisely because
reliability is complementary \citep{kremer1993oring}, applied to governments
choosing whom to negotiate with. Two consequences follow, and they should be
stated together because they pull in different directions. Any estimate recovered
from treated pairs is an effect on self-selected matches and does not license
extrapolation to a pair that has not signed, which limits the external reach of
what the sections below can report. But the same selection is a testable
implication rather than only a limitation: it predicts that the treated
population is more stable in its pre-agreement behavior than the untreated, and
that prediction can be checked without observing $c_{ij}$.

% ¶10 — Reconciles (iii) with (iv): they run on disjoint subsets of the pair's
% conditions, and their conjunction is sharper than either alone.

Predictions (iii) and (iv) may appear to pull against one another, the first
locating the largest effects among small and thinly integrated pairs, the second
locating agreements among pairs whose conditions already hold. They operate,
however, on disjoint parts of $c_{ij}$. Selection runs on the links a treaty does
not address: contractual enforceability, payment and correspondent-banking
infrastructure, the administrative capacity of the counterparty --- the
conditions that make gains foreseeable and therefore worth the cost of
negotiating. Gradation runs on the links a treaty does address: tariffs,
administrative barriers at the border, standards recognition --- the constraints
still binding that an agreement can relax. A pair may have a functioning
non-policy chain, and so be a plausible negotiating partner, while its
policy-addressable constraints remain fully binding and its realized volume
correspondingly low. That combination describes a small, thinly integrated pair
with a competent counterparty, and it is not unusual. The two predictions are
therefore compatible, and their conjunction is sharper than either alone: the
mechanism predicts that the treated population is drawn from the stable end of
the condition distribution and not from the large end, and stability and size are
separable in the data.

% ¶11 — Legacy closing: names the empirical distinction between the two error
% structures and defers it, so the empirical section inherits an obligation.

The distinction between the non-separable form and the additive alternative, in
which the shock enters in parallel to the covariates rather than multiplying
them, is consequential rather than conventional: only the non-separable form
generates dispersion through the interaction of the shock with treatment, and
only it implies that the dispersion of the outcome varies with treatment. That
implication is testable directly, on pairs observed before and after adoption,
and the test bears the weight of everything that follows: if the additive form
survives it, the modulation in Equation~\eqref{eq:nonsep} is a redescription
rather than a mechanism, and the corrections built upon it are unmotivated. We
defer the argument for the non-separable structure, and the test that
distinguishes it from the additive alternative, to
Section~\ref{sec:empirical-dispersion}, where it underpins
the analysis of how the adjustment interacts with the fixed-effects structure.

\subsection{\label{sec:theory-standard}The Evidentiary Standard}

% ¶12 — Sets the abductive standard explicitly, disclaims uniqueness, and hands
% each prediction to a named later section.

Each of these four predictions is weak taken alone, and we ask the reader to hold
the argument to the standard the mechanism actually meets rather than to one it
does not. Lognormal outcomes are overdetermined, as
Section~\ref{sec:theory-distribution} conceded. Right-skewed effects are
consistent with any story in which gains are bounded below and unbounded above. A
size gradient is consistent with diminishing returns of a wholly different
origin, or with measurement error correlated with volume. Selection on
unobservables is consistent with almost any account of why governments negotiate.
What the mechanism claims is not that any one of these is decisive but that their
conjunction is jointly predicted: lognormal outcomes \emph{and} right-skewed
effects \emph{and} effects graded in size and prior integration \emph{and}
selection on the conditions that do the modulating. Each rival that explains one
of these comfortably explains at least one of the others badly or not at all,
and to our knowledge no single alternative currently on the table predicts all
four together. Whether the four can be held jointly against these data is a
separate question from whether the mechanism predicts them jointly, and
Section~\ref{sec:results-reconciliation} reports that only two of the four were
reached. This is abduction, not proof: the conjunction is evidence for the
mechanism in the sense that the mechanism would render it unsurprising and its
rivals would not, and a mechanism we have not considered may do as well or
better. Sections~\ref{sec:empirical} and~\ref{sec:results} take up those of the
four the design can reach --- (ii) and (iii) in the corrected effect distribution
and its variation across pair types, and (iv) in the composition of the treated
population. Prediction (i) concerns the marginal distribution of flows and is not
tested here, for the reason given in the paragraph below.
Section~\ref{sec:conclusion} reports which survived.

% ¶N — States the scorecard in advance so the reader holds the conjunction
%      with the right expectations; mirrors §5.4 and the coda.
We state the outcome in advance, because the conjunction is only as strong as
its weakest leg and the reader should know from the outset which leg that is.
Lognormal-type outcome tails are not tested here. The prediction concerns the
marginal distribution of flows, and this paper's design bears on it only through
the distributional assumption the propositions maintain; that assumption is
examined directly in Section~\ref{sec:empirical}, and it is rejected. Effects
graded in size and prior integration are confirmed, and in the specific form this
mechanism requires --- a steep decline across the less integrated pairs
followed by a common floor, not a smooth monotone descent, which would count
against the mechanism rather than for it. Selection on match quality is
supported: the pairs that adopt are systematically unlike the pairs that do
not, in ways that persist and that the correction layer detects without fully
purging. Right-skewed effects are neither confirmed nor refuted. The shape of
the effect distribution is not identified at the signal share these data
achieve, and we report that as a limit of the measurement rather than as
evidence against the prediction. Two legs carry weight, one is under-identified
and one is untested; we take the conjunction to be informative on that basis and
not on a stronger one.
% =============================================================================
% SECTION 3 — DATA AND THE ESTIMATING POPULATION
% Draft DATA-2 (final). Supersedes DATA-1.
% Hands off from sec:theory-standard (§2.4).
%
% Fact convention: every number carries a trailing "% [ID]".
%   [FROZEN_FACTS$x]  -> REBUILD_V2/code/gates_lib_v2.R, FROZEN_FACTS
%   [TD1R]            -> REBUILD_V2/output/TD1R_population_census.csv
%   [I2] / [I3]       -> Section-3 inputs run, 2026-07-26
% All [NEEDED] markers from DATA-1 are resolved. None remain.
% =============================================================================

\section{\label{sec:data}Data and the Estimating Population}

% ¶1 — Hands off from §2.4: the predictions are obligations on data as much as
% on method, so the construction is fixed before any estimator sees it.

Section~\ref{sec:theory-standard} committed the mechanism to four predictions
and assigned each to a later section. Those assignments are obligations on the
data as much as on the method. A prediction about the shape of outcomes, about
gradation in size and prior integration, and about the composition of the
treated population can be tested only against a population whose construction
is fixed in advance and stated in full, since a construction chosen after the
estimates are seen can accommodate any of them. This section fixes it. We
describe the trade panel and the two configurations in which it is used, the
agreement indicator and how adoption is dated from it, the rules that select
the pairs on which pair-level effects are computed, and the populations from
which the counterfactual is built. Nothing here reports an estimate.

\subsection{\label{sec:data-flows}Trade Flows}

% ¶2 — Source, dimensions, units, frozen counts, and why the window is 1988-2019.

Bilateral trade comes from the International Trade and Production Database for
Estimation, Release~2 \citep{borchert2022international}, aggregated to total goods
trade for each ordered exporter--importer pair and year, in millions of current
United States dollars. The panel runs from 1988 to 2019,
% [FROZEN_FACTS$year_min, FROZEN_FACTS$year_max]
 which is the window over which the release's goods sectors are jointly
available; its services industries begin only in 2000 and are outside the panel
by coverage rather than by choice. After the restrictions described next, the
estimation panel contains $794{,}720$ observations % [FROZEN_FACTS$n_obs]
across $46{,}803$ ordered pairs, % [TD1R, R0]
 of which the terminal cross-section contributes $31{,}650$.
% [FROZEN_FACTS$n_2019]
 These counts and the year bounds are frozen: every script verifies them
against the source file before computing anything and halts if they do not
reproduce.

% ¶3 — The two configurations, stated once, so no later re-inclusion of
% domestic flows reads as inconsistency.

Two configurations of this panel are used, and we state both here. Estimation
of the agreement effect uses positive international flows: observations in
which exporter and importer coincide are excluded, and observations recording
zero trade are excluded with them. The first follows from what is being
estimated --- an agreement acts on trade between two countries, and a country's
trade with itself carries no agreement --- and the second from the pair-level
construction, which takes logarithms of realized flows and their counterfactual
counterparts. The general-equilibrium analysis of
Section~\ref{sec:propagation} re-introduces domestic flows, because its
accounting requires them: an expenditure system in which each economy's outlay
must exhaust across all sources cannot be closed on international flows alone.
The two are not competing samples but the input requirements of two different
calculations.

% ¶4 — What a zero is in this release; bears on the intensive-margin restriction.

One property of the release deserves recording where the exclusions are stated
rather than buried. Missing international flows in Release~2 are set to zero on
the reasoning that reported trade is comprehensive and the bilateral matrix is
genuinely sparse. A zero here is therefore an observed absence of recorded
trade rather than a measured value of nothing, and dropping zeros drops both.
We take no position on the imputation and note only that it makes the boundary
between a pair that does not trade and a pair whose trade is unrecorded less
sharp than the data's appearance suggests.

% ¶5 — Why gross bilateral flows suit the estimand. No distributional commentary.

The outcome is the gross value of goods shipped from one member of the pair to
the other. This is the right object rather than merely the available one. The
treaty is a bilateral instrument: it is signed by two governments, it alters
the terms on which exporters in one meet buyers in the other, and the unit at
which its provisions bind is the pair. An outcome on any other unit would
either aggregate across pairs the treaty treats differently or decompose the
pair into components the treaty does not address. Value-added measures
reallocate part of a bilateral relationship's response to the third countries
supplying its inputs, which is informative about where production sits but not
about what the agreement did to the relationship it governs; netting flows
within a pair discards the directional asymmetry the provisions themselves
distinguish. Gross directed flows keep estimand and instrument on one unit.

\subsection{\label{sec:data-agreements}Agreements}

% ¶6 — The indicator, the classification, and the exclusions with their reasons.

Agreement status is the binary indicator of Egger and Larch
\citep{EggerLarch2008}, recording for each pair-year whether a reciprocal
agreement is in force. Pairs are classified from the time path of this
indicator alone. A pair is a \emph{switcher} if the indicator makes exactly one
transition from zero to one and none from one to zero; its adoption year is the
first year the indicator equals one. Pairs whose indicator equals one in every
year are always-treated: they carry no within-pair variation and cannot
contribute to a within-pair design, whatever their weight in world trade. Pairs
recording more than one transition are excluded, because adoption is not well
defined for them and a pair-level effect computed across their regimes would
average over the very heterogeneity the paper measures. Together these
exclusions are large: $6{,}339$ of $46{,}803$ pairs are single switchers,
% [TD1R, R1]
 and the $40{,}464$ removed are overwhelmingly never-treated.

% ¶7 — Back-reference to §2.3's passage on instrument depth. No new claims.

The indicator is binary, and Section~\ref{sec:theory-nonsep} has already
observed that a single indicator bundles legal instruments ranging from bare
tariff schedules to architectures governing services, procurement, standards
and dispute settlement; the construction described here does not separate
variation originating in that bundling from variation originating in pair
conditions, and we make no claim about their relative contribution.

\subsection{\label{sec:data-population}The Estimating Population}

% ¶8 — The design logic that generates the rules.

The design is within-pair: each pair's effect is identified from the comparison
of its own realized trade after adoption with what the fitted model implies for
it in the absence of the agreement, so the pair is its own control and no
cross-pair comparison enters the pair-level estimate. Every selection rule
follows from that design, and each is stated with the reason it exists rather
than as a threshold applied by convention. Table~\ref{tab:TD1} reports the
pairs surviving each rule in the order applied.

% ¶9 — Rules (a)-(c).

The rules are: (a) \emph{switchers only}, since the within-pair comparison
requires observation on both sides of adoption, and always-treated and
multi-switcher pairs are excluded by
Section~\ref{sec:data-agreements}. (b) \emph{adoption within 1991--2016}: a
pair adopting in the panel's first or last years cannot satisfy the window
requirements below, and the range makes the boundary explicit rather than
implicit; it removes $753$ pairs. % [TD1R, R2]
 (c) \emph{a symmetric one-year exclusion band around adoption}. The
pre-period ends at $\text{adoption}-2$ and the post-period begins at
$\text{adoption}+2$; the adoption year and both flanking years are assigned to
neither side. Agreements are announced before they enter force and implemented
in stages after, so trade in the immediate neighbourhood of the switch belongs
cleanly to neither regime. The band is symmetric by construction, and the same
band governs the comparison populations of
Section~\ref{sec:data-comparison}, so that treated and untreated pairs are
never compared across differently drawn windows.

% ¶10 — Rules (d)-(e), with the intensive-margin consequence stated honestly.

Two further rules concern estimability and window length. (d) \emph{a usable
counterfactual}: a pair enters only if the imputation model of
Section~\ref{sec:empirical} yields a prediction for its post-adoption cells,
which requires that its own fixed effect be identified from untreated data.
This removes $576$ pairs; % [TD1R, R3]
 because identification binds at the pair fixed effect rather than cell by
cell, every retained pair has a counterfactual for all of its post-adoption
years, and no retained pair is estimated on a truncated window. (e) \emph{at
least three pre- and three post-adoption years of positive trade}, outside the
exclusion band, removing $353$ and $475$ pairs respectively.
% [TD1R, R4 and R5]
 A pair-level effect estimated on one or two years is dominated by whichever
shocks fall in them, and the minimum buys enough within-pair observations for
the effect and its sampling error to be separately estimable. The minimum is a judgment rather
than a discovery: it trades sample size against within-pair precision, and we do
not claim to have resolved that trade-off here.

% ¶11 — What rule (e) implies about the margin, stated plainly.

Rule (e) carries a consequence that should be stated rather than left to be
discovered. A pair recording no positive trade in any pre-adoption year has no
pre-period and is removed by the rule mechanically; there is no separate
extensive-margin exclusion because none is needed. The effect this paper
estimates is therefore an intensive-margin effect --- what an agreement does to
trade between countries already trading --- and it is silent on the creation of
trading relationships where none existed. That is a real restriction on the
estimand's reach, since the excluded pairs are precisely those for which an
agreement might matter most in proportional terms.

% ¶12 — The resulting population, its weight, and its composition, honestly.

Applying these rules yields the canonical population of $4{,}182$ pairs,
% [TD1R, R5]
 with adoption years from 1992 to 2015. The population is economically
substantial: its members account for $22.1$ percent of world international
goods trade in 2015. % [I2]
 Its composition, however, deserves comment. The largest members by
pre-adoption trade are the Korea--United States agreement of 2012, the
Australia--Japan and Australia--China agreements of 2015, the China--Hong Kong
arrangement of 2003, and the Japanese agreements with Thailand, Indonesia and
Singapore; % [I3]
 the ten largest directed pairs account for $34.3$ percent of the
population's pre-adoption trade. Conspicuously absent are the North American
and European agreements a reader might expect. They are absent by construction
rather than by selection: Canada and the United States were already in force
under their 1989 agreement when the panel opens, and intra-European pairs are
in force throughout, so all are always-treated and carry no within-pair
variation to identify from. The population is thus weighted toward the
Asia-Pacific agreements of the 2000s and 2010s, and results should be read as
speaking to agreements of that kind.

% ¶13 — The frozen estimand sentence, verbatim.

With the population fixed, the object of estimation can be stated exactly. It
is the distribution across adopting pairs of the pair-level multiplicative
effect of a new agreement on bilateral trade, for pairs with pre- and
post-adoption trading histories.

\subsection{\label{sec:data-comparison}The Comparison Populations}

% ¶14 — Never-treated pairs and switchers' own pre-periods, introduced as data;
% roles named, not performed. Also names the estimation sample, which is their
% union - the fact that matters for §4 and is a data-construction fact here.

Two further populations are constructed here and used later. The first is the
never-treated pairs: those whose indicator is zero throughout, to which
pseudo-adoption years are assigned under the same band and minimum-length rules
that govern the switchers. Because nothing happens to these pairs at their
assigned dates, whatever a pair-level effect returns for them is by
construction not an agreement effect, and the population therefore serves as a
reservoir against which the machinery can be calibrated. The second is the
switchers' own pre-adoption periods, which record how each pair behaves in the
absence of the treatment it later receives, and so allow the pairs to calibrate
themselves rather than each other.

% ¶15 — Their union is the estimation sample. Stated as a data fact, with the
% estimator itself deferred to §4.

These two populations have a further role that belongs here rather than later,
because it determines which observations are used at all. Their union --- every
year of every never-treated pair, together with every pre-band year of every
switcher --- is the sample on which the counterfactual is fitted. No cell in
which an agreement is in force enters that fit. The consequence is that a
switcher's post-adoption counterfactual is a prediction for years the fitting
sample never saw, rather than a fitted value for years it did; what that buys,
and what it costs, is the business of Section~\ref{sec:empirical}. We record
here only the sample definition, since it is a property of the data
construction and not of the estimator applied to it.

% =============================================================================
\begin{table}[htbp]
\centering
\caption{Construction of the estimating population}
\label{tab:TD1}
\begin{tabular}{llrr}
\toprule
 & Rule & Pairs & Removed \\
\midrule
     & All ordered pairs, international, positive trade & $46{,}803$ & --- \\
\addlinespace
(a)  & Single switchers (one $0\!\to\!1$ transition)    & $6{,}339$  & $40{,}464$ \\
(b)  & Adoption year in $[1991, 2016]$                  & $5{,}586$  & $753$ \\
(d)  & Usable counterfactual from untreated data        & $5{,}010$  & $576$ \\
(e1) & $\geq 3$ pre years, outside the band             & $4{,}657$  & $353$ \\
(e2) & $\geq 3$ post years, outside the band            & $4{,}182$  & $475$ \\
\midrule
     & \textbf{Canonical population}                    & $\mathbf{4{,}182}$ & \\
\bottomrule
\end{tabular}

\vspace{0.5em}
\begin{minipage}{0.95\textwidth}
\footnotesize
\emph{Notes.} Rules are applied in the order shown. Rule~(c), the symmetric
one-year exclusion band, is not a separate row: it defines the pre- and
post-periods over which (e1) and (e2) count years, so its effect is embedded
in those two rows. Pre years are those before $\text{adoption}-1$ and post
years those after $\text{adoption}+1$, both requiring positive trade. Panel
totals are frozen and verified at load.
% [FROZEN_FACTS$n_obs]
 Source:. % [TD1R]

\end{minipage}
\end{table}

% =============================================================================
% SECTION 4 — THE ANATOMY OF THE ARTIFACT
% Draft SECTION4-1 (final). Hands off from sec:data-comparison (§3.4).
%
% Fact convention: every number carries a trailing "% [ID]".
%   [T5R: col]      -> output/T5R_theta_summary.csv (BASELINE row, n=4182)
%   [T21: arm]      -> output/T21_arms.csv
%   [T22 / <ID>]    -> output/T22_reliability.csv, IDs as in meta/canonical_facts.md
%   [T23: row,col]  -> output/T23_anchor.csv (anchor table, D2)
%   [T24: row]      -> output/T24_placebo_uncorr.csv
%   [T9: <row>]     -> output/T9_placebo_holdout.csv
%   [T11: item]     -> output/T11_se_decomposition.csv
%   [MEAN_THETA_D], [SD_THETA_TRUE], [SPLITHALF_A_R], [SPLITHALF_A_RELIABILITY],
%   [PLACEBO_A_MEAN], [PLACEBO_A_SD], [PLACEBO_A_R], [PLACEBO_A_RELIABILITY]
%                   -> meta/canonical_facts.md (SOLE AUTHORITY)
%   [INV-nnn]       -> meta/SUPERSEDED.md invalidation register
%   [Prop1] [Prop1c] [Cor:rel] [Prop2] [Prop3] [Cor:rhoop] -> article/main.tex appendix
%
% Verified against repo HEAD (main, post-SYNC-6e). No open ledger-vs-output
% disagreements and no [NEEDED] markers remain; see D4 at the foot of this file.
% =============================================================================

\section{\label{sec:empirical}The Anatomy of the Artifact}

% ¶1 — Reports the naive estimate deadpan. No foreshadowing; the reader is meant
% to find it credible on the diagnostics standard practice would apply.

The counterfactual of Section~\ref{sec:data-comparison} makes a pair-level
estimate available without further apparatus. For each switching pair, take the
untreated-only PPML fit, impute the trade the pair would have carried in each of
its post-adoption years, and average the log gap between realized and imputed
trade over those years. Call this Definition~$A$. Across the canonical
population it returns a mean of $-0.2837$,
% [T5R: Mean_theta_A]
 a standard deviation of $1.6630$,
% [T5R: SD_theta_A]
 and a split-half correlation across disjoint halves of each pair's post window
of $\LedgerSplithalfR$, on the $4{,}120$ pairs with at least two post-adoption years in
each half,
% [SPLITHALF_A_R]
 or $0.9607$ once corrected for split length.
% [SPLITHALF_A_RELIABILITY]
 The estimator is thus sign-wrong against everything the aggregate literature
reports, enormously dispersed, and---by the one diagnostic that pair-level
estimation makes available---close to perfectly reliable.

% ¶2 — Applies the identical machinery to a population where the answer is known
% in advance. This is the section's thesis paragraph.

The never-treated pairs of Section~\ref{sec:data-comparison} carry
pseudo-adoption dates assigned under the same band and minimum-length rules, and
nothing whatever happens to them at those dates. Run Definition~$A$ on them
unchanged. On the $15{,}683$ never-treated pairs that qualify for the same split, the mean
``effect'' is $-0.6821$,
% [PLACEBO_A_MEAN]
 with a standard deviation of $1.0814$
% [PLACEBO_A_SD]
and a split-half correlation of $\LedgerPlaceboR$,
% [PLACEBO_A_R]
 or $0.8547$ corrected for split length.
% [PLACEBO_A_RELIABILITY]
 A population in which the effect is zero by construction thus reproduces
roughly four-fifths of the treated estimator's reliability---$\LedgerPlaceboR$ against
$\LedgerSplithalfR$, computed under the same split rule in the same run
% [PLACEBO_A_R; SPLITHALF_A_R]
---and rather more than twice its mean in magnitude.

% ¶3 — Names the mechanism that makes the paradox general rather than an
% accident of this design, and hands the exact statement to the appendix.

The reason is not subtle once stated, and it is a property of the diagnostic
rather than of this application. Split-half reliability asks whether a pair's
estimate in one half of its post window predicts its estimate in the other. Any
quantity that is a persistent trait of the pair will answer yes. The artifacts
at work here---each pair's own volatility, and each pair's own trend relative to
its fitted level---are exactly such traits: they are properties of the
relationship, not of the years sampled from it, so they replicate across
splits as faithfully as a real effect would. Proposition~\ref{prop:jensen}(c)
gives the exact result under the stated assumptions: the split-half correlation
of a zero-effect estimator equals the share of its variance carried by
persistent bias, $\Var(b)/\big(\Var(b)+\E[\sigma^2]/T_h\big)$.
% [Prop1c]
 Corollary~\ref{cor:rel} states the consequence without reference to any
particular artifact: \emph{any} dispersed, persistent per-pair bias passes.
% [Cor:rel]
 High reliability certifies persistence. It does not certify validity, and no
amount of it can, because the two are different questions and only one of them
is being asked.

% ¶3a — Bounds the claim before the anatomy begins, so that the section is not
% read as asserting that the treated estimate is entirely artifact.

It is worth being precise about what the placebo population establishes and what
it does not. It establishes that the machinery, applied to pairs where the
answer is known to be zero, returns numbers that are large, dispersed, and
stable across resplits, and that the diagnostics conventionally used to
adjudicate such estimates do not distinguish that case from a real one. It does
not establish that the treated estimates are artifact; the treated pairs
received agreements and the never-treated pairs did not, and nothing in the
comparison speaks to what the agreements did. What the comparison does is set
the standard the treated estimate has to clear, which is not zero but whatever
the same machinery returns when there is nothing to find. The rest of this
section is the accounting that clears it, layer by layer, and the accounting is
worth doing precisely because the diagnostics that would ordinarily settle the
matter have been shown not to.

% ¶4 — Opens the anatomy and states the justification standard that governs all
% three layers, with the fixed-effects contrast made once.

What follows takes the naive estimate apart into three layers. Each has a
mechanism with a derivation, a correction, and a population in which effects are
absent by construction and the mechanism is therefore visible on its own. That
last requirement is the justification standard we hold the corrections to
throughout: a subtraction is licensed when there exists a population where the
quantity subtracted is all there is to find. The contrast with the conventional
instrument is worth making once. Fixed effects remove identifying dimensions
before estimation, and what they remove is unavailable for inspection
afterwards; the corrections below remove what the estimator is demonstrated to
report when the effect is zero, and the demonstration is a table one can read.

% ¶5 — Layer (i): the mechanism and its exact form.

The first layer is aggregation. Definition~$A$ averages log ratios, and the
logarithm is concave, so a pair whose realized trade is volatile around its
counterfactual is penalized for the volatility itself. Under the assumptions of
the appendix and with zero treatment effect,
$\E[\theta^A_{ij}\mid ij]=-\sigma^2_{ij}/2$ exactly:
% [Prop1]
 the estimator returns a negative number whose magnitude is half the pair's own
log-variance and has nothing to do with any agreement. The remedy is to
aggregate before taking the logarithm rather than after---the log of the ratio
of sums over the post window, Definition~$B$---which replaces a pair-level
penalty of order $\sigma^2_{ij}$ with one of order
$\Var(R_{ij})=\Var(\eta_{ij})\sum_t w_t^2$, smaller by roughly the number of
post years.
% [Prop1]


% ¶6 — Layer (i) empirics: the sign flip is located, and the residual failure
% that motivates layer (ii) is stated rather than deferred.

The move is decisive for the mean and only for the mean. Across the canonical
population the average effect goes from $-0.2837$ under $A$ to $0.0758$ under % [T23: A,treated_mean; B,treated_mean]
$B$:
 the sign flip happens here, at an arithmetic layer, before any correction that
uses information about untreated pairs. The zero-effect population moves in the
same direction and by more. Measured on the same $\LedgerPlaceboN$ never-treated pairs
by the same producer, its mean rises from $\LedgerPlaceboAMean$ under $A$ to $-0.2072$ under % [T23: A,placebo_mean; B,placebo_mean]
$B$.
 It does not, however, reach zero. Against the validity threshold of $0.05$ % [GATE_THRESHOLD]
carried by the pipeline's gate library, the uncorrected placebo mean under
Definition~$B$ sits at roughly four times the threshold.
% [T24: PLACEBO_B_UNCORR_OVERALL]
 That residual is systematic rather than incidental---it is a property of the
population and the window, not of any resampling---and the second layer is what
it is made of.

% ¶7 — Layer (ii): the drift mechanism, with its two testable signatures.

The second layer is within-pair drift. Trade relationships do not hold station
around their fitted level; they fade or strengthen relative to it, and a pair
fixed effect centers the level without touching the trend. Any comparison of a
late window to an early one therefore reads a fading relationship as a negative
effect. Proposition~\ref{prop:drift}(a) makes the size of the misreading
explicit: for a never-treated pair with a post window equal to the last
$T_{\mathrm{post}}$ years of its span, the post-window mean of centered time is
$T_{\mathrm{pre}}/2$, so
$\E[\theta^{A}_{ij}]=-\sigma^2_{ij}/2+\delta_{ij}T_{\mathrm{pre}}/2$.
% [Prop2]
 Two signatures follow and are jointly restrictive. The bias depends on window
geometry alone and never on calendar time, so it cannot be a macroeconomic
episode in disguise. And where drift correlates with pair size, the bias is
graded in size---which is what makes a size-cell correction the right shape of
instrument rather than an arbitrary one.
% [Prop2]
% NOTE (deliberate, not outstanding): the empirical size profile is monotone
%  only over the upper deciles. T24 gives placebo-B means of -0.365 at decile 5
%  falling to -0.057 at decile 10, but -0.206 at decile 1 and -0.217 at decile 2,
%  so the smallest pairs are NOT the most biased and the profile is hump-shaped.
%  The sentence above therefore states the grading conditionally, exactly as
%  Proposition 2 derives it, and asserts no empirical monotonicity. The
%  propositions appendix reports the full profile.

% ¶8 — Layer (ii) correction and its own validation, including the honest partial.

The correction estimates, within size cells, the mean effect the machinery
returns on never-treated pairs, and subtracts it. Two features make this more
than a relabeling. It is estimated on held-out placebo splits and applied out of
sample, so no pair contributes to its own correction. And it is validated on the
held-out half, where the corrected placebo effect should be zero if the
subtraction removed an artifact and nothing else. It is: across $8{,}545$
validation pairs the mean corrected placebo effect is $0.0188$ with a standard % [HOLDOUT_MEAN]
error of $0.0087$,
 inside the $0.10$ decile bound of the pipeline's three-state gate and inside % [GATE_DECILE_BOUND]
the stricter $0.05$ bound as well.
% [INV-016]
 One of the ten size deciles does not clear the first bound. The third decile
returns $-0.1013$ on $314$ validation pairs, with a standard error of $0.0591$,
% [T9: decile 3]
 which registers as PARTIAL under the three-state rule---beyond $0.10$, well
inside $0.20$, and $1.7$ standard errors from zero.
% [INV-016]
 We report it as such rather than absorbing it into an average that would hide
it. Applying the correction moves the mean from $0.0758$ under $B$ to $\LedgerMeanThetaD$ % [T5R: Mean_theta_B; MEAN_THETA_D]
under $D$, with a standard error of $\LedgerMeanThetaDSE$.


% ¶8a — Explains the resolution of the correction and marks where it stops,
% which is the hinge into layer (iii).

The unit of the correction is worth dwelling on, because it is also the source
of the layer's limitation. Proposition~\ref{prop:drift}(c) is explicit that
subtracting a size-cell mean removes $\E[\delta_{ij}\mid s]T_{\mathrm{pre}}/2$
and leaves the within-cell dispersion of drift untouched, so that
$\Var(\theta^D\mid s)$ still contains $(T_{\mathrm{pre}}/2)^2\Var(\delta_{ij}\mid
s)$ alongside the true effect variance and the sampling term.
% [Prop2]
 A finer partition would remove more of it. The binding constraint is that the
correction must be estimated on held-out placebo pairs and validated on them,
and the validation folds are already thin at the current resolution: the
smallest size decile contributes $19$ validation pairs and the second $94$,
% [T9: deciles 1 and 2]
 against $1{,}365$ in the eighth. Refining the partition would buy a smaller
residual at the cost of a correction no held-out population is large enough to
certify, which is the trade the current design declines to make. The consequence
is carried forward: whatever drift heterogeneity survives within cells is still
in the dispersion, and the next layer has to account for it rather than assume
it away.

% ¶9 — Layer (iii) opens by showing that the first two layers did nothing to
% dispersion, which is the fact that organizes the rest of the subsection.

The two corrections between them move the mean by roughly half a log point. They
do almost nothing to the spread. The standard deviation is $1.6630$ under $A$, % [T5R: SD_theta_A]
$1.5799$ under $B$, and $1.5614$ under $D$
---a decline of about six percent across a path that reversed the sign of the
mean. This is not a failure of the corrections. It is a statement about what
they were built to remove: both are corrections to a location, the first to a
pair's own expected penalty and the second to a size cell's average drift, and
neither has any purchase on variation across pairs within a cell. The dispersion
that remains is a different object and has to be accounted for separately.

\subsection{The null accounting}
\label{sec:empirical-dispersion}

% ¶10 — Layer (iii): the null accounting, arm by arm, with each arm's assumption
% named where its number is given.

That accounting proceeds by asking how much of the observed dispersion a
zero-effect population reproduces, and the answer depends on which zero-effect
population one is entitled to use. Against a total variance of $2.4380$,
% [T21: VAR_THETA_D]
 three admissible nulls have been computed. Sampling noise from finite post
windows, augmented by the uncertainty in the estimated counterfactual itself,
accounts for $0.2611$, or $11$ percent;
% [T21: A_noise_only]
 the counterfactual component is a third of that total and cannot be dropped.
% [T11: share_sd_cf; INV-022]
 The in-sample placebo null---the dispersion the machinery generates on
never-treated pairs over the same years---accounts for $0.6796$, or $28$ percent.
% [T21: B_placebo]
 The out-of-sample drift null, computed under the canonical symmetric-window
design so that treated and pseudo-treated pairs are compared across identically
drawn windows, accounts for $1.887$, or $77$ percent.
% [T21: C\_OOS]


% ¶10a — Prevents the arithmetic misreading of the three shares and states the
% ordering as a consequence of what each null admits.

These three shares do not partition anything and must not be added. The nulls
are nested by construction: the first admits sampling variability and the
uncertainty of the imputation and nothing else; the second admits those together
with whatever the machinery generates on pairs that received no agreement over
the same calendar years; the third admits those together with the
window-geometry drift of Proposition~\ref{prop:drift}, measured on pseudo-treated
pairs whose windows are drawn as the treated pairs' windows are. Each is the
previous one plus a further admitted source, which is why they run $11$, $28$,
and $77$ percent rather than in any other order,
% [T21: all arms]
 and why the middle one is an interior value rather than an endpoint. Choosing
among them is choosing how much of the observed spread one is prepared to
concede to a mechanism that produces spread without producing effects, and the
propositions do not settle that choice---they establish what is at stake in it.

% ¶11 — Converts the accounting into the reported conclusion and licenses the
% interval form by Proposition 3's logic rather than by caution.

The first and third of these are the admissible endpoints, and they bracket the
standard deviation of true effects at $[\LedgerSDTrueLo,\,\LedgerSDTrueHi]$.
% [SD_THETA_TRUE / INV-027]
 We report the interval and no point inside it, and the reason is not caution
but Proposition~\ref{prop:ident}. Part~(a) identifies the variance of persistent
effects as the cross-pair variance of the estimate minus the expected
within-pair variance over the post window, which is estimable.
% [Prop3]
 Part~(b) shows that the composition of that within-pair variance is not:
under the stated normality, transitory effect heterogeneity and a
treatment-coincident change in volatility enter the likelihood only through
their sum, so any two parameterizations with equal composites generate identical
data distributions.
% [Prop3]
 The split between them is a modeling label, not an estimable quantity, and the
two endpoints are what the two admissible labels imply. The interval is
therefore the dispersion conclusion of this section, with each arm's assumption
attached to it: the upper endpoint concedes only noise, the lower concedes
everything an out-of-sample zero-effect population reproduces.
One feature of the lower endpoint deserves emphasis, because it is what makes
that endpoint conservative rather than merely small. The out-of-sample null is
not formed by applying an already-fitted counterfactual to held-out years. The
counterfactual is re-estimated from a sample from which the evaluation window
has been excluded entirely, so that no cell used to judge the null contributed
to the fit that generates it.
% [INV-027a]
 This matters more than it may appear. A null built the other way---reusing a
counterfactual fitted on the very cells it is then evaluated against---reproduces
substantially less of the observed dispersion, because the fit has already
absorbed part of what the null is meant to measure, and it would place the lower
endpoint considerably higher. We report the construction that is harder on the
result.

\subsection{The certificate}
\label{sec:empirical-certificate}

% ¶12 — States what the corrections must be shown NOT to do, and names the two
% instruments before running either.

A correction that removes an artifact and a correction that removes the signal
look identical in a table of means. The distinguishing evidence has to come from
data whose truth is known in advance: the propositions are evaluated
numerically, with parameters planted and recovered, and the evaluation is run
at the parameter values where the derived approximations are supposed to fail
as well as where they are supposed to hold. What that establishes is that the
arithmetic underlying each correction is sound and that its stated failure modes
occur where the derivation says they will.

% ¶13 — Instrument (a): the verification, including the diagnostic value of the
% one variant that collapses.

The first is available in full. Each proposition is evaluated numerically at the
ledgered dispersion, which the zero-effect population identifies directly, since
the mean log-ratio there is exactly minus half the mean log-variance.
% [T25: PROP_ESIGMA2]
 Three of the evaluations plant a parameter and recover it. The Jensen
expansion is checked where it is weakest: at the empirical scale the generic
second-order form is invalid for Definition~$A$ altogether, and for
Definition~$B$ it overstates the bias magnitude by about thirty percent, the
positive third-order term partially offsetting the variance term.
% [T25: PROP_APPROX_B, PROP_MC_B, PROP_OVERSTATE_PCT]
 The drift formula is checked by planting a per-pair trend and recovering the
window-geometry prediction.
% [T25: PROP_V2_PRED, PROP_V2_SIM]
 And the reliability decomposition is over-identified, which permits a check
with nothing fitted to it: the cross-pair variance of the estimator on the
zero-effect population implies a dispersion of pair volatilities, and
substituting that into the split-half formula \emph{predicts} a reliability of
$0.754$ against the $0.746$ observed.
% [R_PRED, PLACEBO_A_R]
 The formula reproduces the observed figure to within eight thousandths, with no
parameter chosen to make it do so. The prediction is evaluated pair by pair,
which the convexity of $1/T$ requires: post windows differ across pairs, the
reciprocal of the mean window length is not the mean of the reciprocals, and a
plug-in evaluation at the mean over-predicts, returning $0.807$ and a gap eight % [T25: PROP_R_PRED_PLUGIN]
times larger.


% ¶13a — The variant that fails, which is the sharper half of the certificate.

The most informative check is of an estimator variant that does not work. A null
calibrated on a window shorter than the window over which the estimate is formed
over-subtracts by a computable factor $\kappa_w:=T_{\mathrm{post}}/T_0>1$,
biasing the recovered variance by
$-(\kappa_w-1)\E[V^{\mathrm{post}}]/T_{\mathrm{post}}$.
% [Cor:rhoop]
 The consequence is exact and can be read off. At $\kappa_w=1$ the subtraction
is the noise-only arm and returns its dispersion of $1.475$ by construction; at % [SD_THETA_TRUE]
$\kappa_w=2,3,5$ the recovered dispersion falls to $1.384$, $1.286$ and $1.064$; % [T25: PROP_TAU_K2, PROP_TAU_K3, PROP_TAU_K5]
and at $\kappa_w=9.34$ the entire estimated heterogeneity is absorbed.
 A mis-specified null window therefore consumes true heterogeneity at a rate the
derivation gives in closed form, and a sufficiently mis-specified one consumes
all of it while returning a number that looks like an answer. That the failure
occurs at the magnitude the derivation predicts is a stronger statement than a
variant that succeeds, and it is the reason the null used above is matched to the
treated design rather than to convenience.

% ¶13b — The maintained distributional assumption, tested against a matched
% control rather than against a theoretical normal.
% LEDGER: A2_* (SYNC-13); INV-041 records the location-restriction residual.

One further check bears on the premise rather than the arithmetic. The
propositions are derived under a distributional assumption --- that the log gaps
are normal within a pair --- and an assumption of that kind is testable on the
untreated cells, where the effect is zero by construction. It is tested here
against a matched normal control rather than against a theoretical one, because
centering and scaling within a pair over a short window deforms the distribution
even when the underlying data are exactly normal; the control is drawn pair by
pair with identical cell counts and passed through identical code, so any
deformation the procedure introduces appears on both sides and cancels. On
$27{,}782$ pairs the assumption is rejected: $32.8$ percent of pairs reject % [A2_REJECT_REAL]
normality at the five percent level against $5.0$ percent for the control.
 The direction matters more than the rejection. The departure is left-skewed and
leptokurtic --- skewness $-0.476$, excess kurtosis $0.784$ against the control's % [A2_SKEW, A2_EXKURT]
$-0.269$ --- while the \emph{upper} tail is thinner than the control's at the % [A2_EXKURT_CONTROL]
ninety-fifth, ninety-ninth and ninety-nine-and-a-half percentiles.
 What the data carry is not an excess of extreme gains but an excess of
collapses: trade falling far below its counterfactual more often than a normal
law allows.

The consequence for what precedes it is smaller than the rejection suggests, and
worth stating in those terms rather than defended. The reliability prediction
above is derived exactly under normality, and it lands within eight thousandths
on the same data in which normality is rejected at a third of pairs. A prediction
that survives the failure of its own premise is evidence about the mechanism it
describes rather than about the premise, which is the reason this section reports
the departure alongside the check instead of in place of it. Where the assumption
does bind is on the identification boundary rather than on any quantity reported
here; Remark~\ref{rem:moments} states what the measured departure recovers there,
and the section on the boundary of learnability carries it forward.

% ¶14 — States the limit of the certificate honestly, and names the stronger

What this does not establish should be stated with equal clarity. Verifying the
arithmetic of a correction is weaker than verifying the pipeline that applies
it. The stronger test simulates panels carrying a known effect distribution
alongside a known artifact, runs the estimator and both corrections without
modification, and asks whether the recovered distribution matches the planted
one while the planted artifact is returned as zero; a pipeline that passes the
first requirement and fails the second is absorbing signal, and one that passes
the second and fails the first is leaving artifact behind. We report the
propositions' verification and do not claim that end-to-end test, which we
regard as the standard such corrections should eventually be held to rather
than one this paper meets.

\subsection{Diagnostics that survive}
\label{sec:empirical-diagnostics}

% ¶15 — The constructive close, item one: reliability is reported against its
% null, never raw.

The negative result of this section is narrow: reliability statistics answer a
question about persistence and were never answering the question they are
conventionally read as answering. The constructive one is that the same
machinery, pointed at the right comparison, is informative. Five practices
follow, and the first fixes the diagnostic that failed. A reliability
statistic is uninterpretable in isolation and interpretable against its own
null: report the split-half correlation of the estimator on a population where
effects are absent by construction, computed under the identical split rule in
the same run, and read the treated figure against it. The contrast is the
statistic; either number alone is not.

% ¶16 — Items two and three: self-calibrated nulls, and verification on planted
% truth.

Second, a pair's own pre-adoption years are a null it calibrates for itself, and
pseudo-effects estimated on them measure what the machinery returns from that
pair when there is nothing to find. The out-of-sample drift null used above is
this instrument at population scale, and its discipline is that the window
geometry of the null must match the window geometry of the estimate---the
comparison is uninformative, and can be badly misleading, when the two are drawn
differently. Third, verification on planted truth is the certificate a pipeline
cannot supply about itself from observational data alone. Its value is not
confirmation that the estimator works; it is the location of the parameter
region where it stops working, which is knowable in simulation and not knowable
otherwise.

% ¶17 — Item four: identified-set reporting as a positive practice, not a hedge.

Fourth, where point identification fails, the honest object is the identified
set with each arm's assumption labeled, and the labeling is the substance. An
interval reported without its arms is a confession of uncertainty; an interval
reported with them is a statement about which assumption would have to be true
for which endpoint to hold, and it tells a reader who disagrees with one of the
assumptions exactly which number to take. Proposition~\ref{prop:ident}(b) is the
model case, because it establishes that a particular decomposition is
unavailable in exact terms under the model's normality assumption, and only
conditionally recoverable outside it
% [Prop3]: % [see Remark 4]
 no weakening of the assumption that a reader is likely to grant moves that
boundary far, and reporting a point inside it would represent as measured what is
in fact chosen.

% ¶18 — Item five: content-pinning assertions, framed prospectively as the
% general defense against a general failure class.

Fifth and most general, the properties an estimate must satisfy should be
asserted in code rather than checked by reading. A long empirical pipeline
carries a recurring failure class that no amount of care in the substantive
analysis addresses: the wrong-object substitution, in which an estimate of a
different quantity, a different population, or a different window flows into a
slot whose label it matches and is then reported, cited, and built upon. What
defeats it is not vigilance but assertion---automated gates on properties the
wrong object cannot satisfy. A truncation bound the quantity must respect. A
magnitude scale it must lie on. A sample size, a window length, a standard error
inconsistent with the source the value claims to come from. Such gates are cheap
to write and they fail loudly, which is the only property that matters, because
the wrong object's distinguishing feature is that it is plausible. Taken
together the five are a single standard: every number reported about an effect
should be accompanied by what the same machinery returns when there is no effect
to find, and the accompaniment should be enforced rather than remembered.

% =============================================================================
% D2 — CAPTION FOR THE CORRECTION-PATH ANCHOR TABLE
% =============================================================================

\begin{table}[htbp]
\centering
\caption{The correction path, with zero-effect benchmarks}
\label{tab:correction}
% Body: output/T23_anchor.csv (producer S24b_anchor_table.R).
% Memo row 1: output/T9_placebo_holdout.csv, row NA.
% Memo row 2: output/T22_reliability.csv (SPLITHALF_A_R, PLACEBO_A_R) with n.
% Standard errors are SD/sqrt(n); this reproduces the ledgered SE of
% MEAN_THETA_D (0.0241) exactly, so the column is a transformation of two
% committed columns rather than a separate estimate.
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{3}{c}{Switching pairs} & \multicolumn{3}{c}{Zero-effect benchmark} \\
\cmidrule(lr){2-4}\cmidrule(lr){5-7}
Definition & Mean & SD & $n$ & Mean & SD & $n$ \\
\midrule
$A$: mean of log ratios     & $-0.2837$  & $1.6630$ & $4{,}182$ & $\LedgerPlaceboAMean$  & $\LedgerPlaceboASD$ & $\LedgerPlaceboN$ \\ % [T22/T23: A]
                            & $(0.0257)$ &          &           & $(0.0085)$ &          &            \\
\addlinespace
$B$: log of ratio of sums   & $0.0758$   & $1.5799$ & $4{,}182$ & $-0.2072$  & $0.8244$ & $17{,}200$ \\ % [T23: B]
                            & $(0.0244)$ &          &           & $(0.0063)$ &          &            \\
\addlinespace
$D$: $B$ less cell drift    & $\LedgerMeanThetaD$   & $1.5614$ & $\LedgerN$ & ---        & ---      & ---        \\ % [T23: D]
                            & $(\LedgerMeanThetaDSE)$ &          &           &            &          &            \\
\midrule
\multicolumn{7}{l}{\footnotesize\emph{Memo.} Held-out corrected placebo under $D$: $0.0188$ $(0.0087)$, $n=8{,}545$.} \\ % [HOLDOUT_MEAN]
\multicolumn{7}{l}{\footnotesize\phantom{\emph{Memo.}} Split-half reliability under $A$: $\LedgerSplithalfR$ $(n=4{,}120)$ against $\LedgerPlaceboR$ $(n=15{,}683)$.} \\
\bottomrule
\end{tabular}
 
\vspace{0.5em}
\begin{minipage}{0.95\textwidth}
\footnotesize
\emph{Notes.} Each row is a pair-level effect definition applied to the
canonical population of $4{,}182$ switching pairs,
% [T23: treated_n]
 and each is paired with the same definition applied to never-treated pairs
carrying pseudo-adoption dates, for which the effect is zero by construction
($\LedgerPlaceboN$ qualifying pairs for $A$; $17{,}200$ for $B$).
% [T22/T23: placebo_n; INV-036 -> INV-049]
 Definition~$A$ averages the log ratio of realized to imputed trade over a
pair's post-adoption years. Definition~$B$ takes the log of the ratio of the
sums over the same years, aggregating before the logarithm rather than after,
which removes the concavity penalty of Proposition~\ref{prop:jensen}.
Definition~$D$ subtracts from $B$ the mean placebo effect of the pair's size
cell, estimated on held-out placebo splits and applied out of sample, which
removes the window-geometry drift of Proposition~\ref{prop:drift}. All six
treated cells and all four placebo cells come from a single producer on a single
pair of populations, so the counterfactual, the exclusion band and the
post-window definition are common to every cell shown. The $D$ row has no
placebo entry: the correction is defined by subtracting a placebo mean, so its
zero-effect benchmark is not a column of this table but the held-out validation
of Table~\ref{tab:holdout}, where the corrected placebo effect on $8{,}545$ pairs the
correction never saw is $0.0188$ with a standard error of $0.0087$.
% [T9: NA row]
 Reliability is reported separately, in Table~\ref{tab:reliability}, because it is
computed on the subsets with at least two post-adoption years in each half
($4{,}120$ treated, $15{,}683$ placebo) and so does not share this table's row
denominators; it is reported for treated and placebo as a pair, since the
treated figure alone is uninterpretable---$\LedgerSplithalfR$ against $\LedgerPlaceboR$ under
Definition~$A$.
% [SPLITHALF\_A\_R; PLACEBO\_A\_R]
 Standard errors in parentheses.
\end{minipage}
\end{table}

% =============================================================================
% D3 — 120-WORD SUMMARY FOR THE INTRODUCTION'S CONTRIBUTION PARAGRAPH
% =============================================================================

% Estimating agreement effects pair by pair, the way standard practice would,
% returns a mean of $-0.2837$ with a standard deviation of $1.6630$ and a
% split-half reliability of $0.9243$ % [T5R: Mean_theta_A; T5R: SD_theta_A;
% SPLITHALF_A_R] — a sign-wrong, hugely dispersed estimate that every available
% diagnostic certifies. Applied to never-treated pairs with pseudo-adoption
% dates, where the effect is zero by construction, the identical machinery
% returns $-0.6821$ with reliability $0.7463$ % [PLACEBO_A_MEAN; PLACEBO_A_R]:
% roughly four-fifths of the treated estimator's stability, with no effects
% anywhere. We show why this must be so. Reliability statistics test whether a
% quantity is a persistent trait of the pair, and the artifacts at work —
% pair-specific volatility and pair-specific drift — are persistent traits.
% Corollary~\ref{cor:rel}: any dispersed persistent bias passes. % [Cor:rel]
%
% [121 words excluding fact comments.]

% =============================================================================
% D4 — AMBIGUITIES
% Revised against commit 4b98061 (SYNC-6d), verified directly against the repo.
% =============================================================================
%
% CLOSED — verified in the repo, no action, safe to cite
%
%   Ledger/output split on theta_A moments ....... T22 now carries n; S4.1 and
%       S4.2 state the qualifying counts (4,120 treated, 15,683 placebo) in text.
%   Six placebo counts ........................... INV-036 -> INV-049; now 15,683 qualifying.
%   T7 partial run / extrapolated seed ........... superseded by T24, demoted.
%   Fabricated seed rows in T24 .................. removed; 11 rows.
%   Anchor table did not exist ................... T23, single producer.
%   0.39 / 0.46 in the Prop 3 remark ............. removed; arm-indexed 0.74/1.48.
%   E[sigma^2] derived from the placebo SD ....... corrected to the mean:
%       E[sigma^2]=1.364, sigma=1.168 [T25].
%   [V1c] self-contradiction (r=0.75 in 0.47-0.73)  replaced by a parameter-free
%       over-identification test: predicts 0.807 against 0.746 observed.
%   cor:rhoop keyed to retired injection scenarios  replaced by the closed-form
%       kappa ladder; kappa=1 reproduces arm A exactly [T25: PROP_TAU_K1].
%   "roughly a third" / M1 sentence / R^2<1% ..... removed from main.tex.
%   INV-027 citing superseded producers .......... now S24_arms_canonical.R.
%   INV-027a stated backwards .................... now DECREASES, 0.74 -> ~1.0.
%   INV-035 "Definition D" mislabel .............. now Definition B.
%   Arm naming (C_hardened vs C_OOS) ............. harmonised to T21 identifiers.
%   enforce.R never halted ....................... stopifnot(n_violations == 0).
%   Appendix literals unchecked .................. rule (f) requires a resolving
%       ID; rule (g) requires every \Prop* macro to resolve.
%
% OPEN — none. All items verified closed in the repo (main, post-SYNC-6e).
%
%   T2R sidecar warning ......... corrected; the file no longer asserts
%       Var_theta_hat = Var(theta_D).
%   SUPERSEDED.md ............... T7 entry added; INV split-register note added.
%   PROP_V2_SHIFT ............... 0.0494 -> 0.0010 after the centering-midpoint
%       fix; calendar invariance now genuinely tested.
%   V4 mislabel ................. var_11plus_S18 -> var_11plus_N1b. The 1.23 is
%       N1b's out-of-sample variance; S18's in-sample value is 0.159.
%   T26 ......................... sidecar added, marked IN-SAMPLE PSEUDO NULL /
%       NOT Arm C.
%   PropTauKone ................. 1.4754, matching T21 SD_true_A exactly.
%
% CLOSED BY INVESTIGATION
%
% G2. RESOLVED — NOT A DEFECT. INV-027a recorded the 11+ horizon bin as carrying
%     two irreconcilable values (0.159 against 1.230) and marked it BLOCKING.
%     The two are different objects, correctly labelled only after the fact.
%     S18 and V4 reuse S1R's fitted y_hat_0, which was estimated on the very
%     late-pre cells they then evaluate: their null is IN-SAMPLE, and its
%     weighted value is 0.389. N1_oos_null.R re-estimates the counterfactual on
%     never-treated pairs plus switchers' early-pre cells only, under an assertion
%     that no late cell enters the estimation sample, and evaluates the late
%     cells out of sample: its weighted value at symmetric-window treated weights
%     is 1.887. Arm C is defined as the out-of-sample drift null, so the
%     out-of-sample construction is the correct object and 1.887 stands.
%     SD_THETA_TRUE = [0.74, 1.48] is unchanged and no arm value moved.
%     The residual defect was reproducibility, not correctness: 1.887 was
%     assembled across two scripts with no single producer, which is why it could
%     not be reproduced from either alone. S24_arms_canonical.R now computes it
%     explicitly from T12_N1b_horizon.csv variances and symmetric-window weights
%     under gate G4. Paragraph 11 states the out-of-sample construction as the
%     conservative choice it is.
%
% G1. CLOSED BY DECISION (2026-07-27). The end-to-end injection certificate is
%     not built and will not be. Paragraph 12 now describes one instrument
%     rather than two, and paragraph 14 states the end-to-end test as the
%     standard such corrections should be held to rather than one this paper
%     meets. Nothing in the section claims it. The planted-truth element that
%     remains is real: the Jensen and drift verifications each plant a parameter
%     and recover it % [T25: PROP_MC_B, PROP_V2_SIM], and the cor:rhoop ladder
%     shows the failure mode occurring at the derived magnitude.
%

% =============================================================================
% SECTION 5 — WHAT SURVIVES
% Drafted against meta/canonical_facts.md at commit 42f2e83 (SYNC-8).
% Every number carries a % [FACT_ID] comment. Nothing here is from memory.
% D2 captions, D3 reconciliation note and D4 ambiguities follow the section.
% =============================================================================

\section{What Survives}
\label{sec:results}

% ¶1 — The headline mean, with the level-scale caveat stated once and not repeated.
Table~\ref{tab:effect-distribution} collects what the corrected distribution
identifies. Across the $\LedgerN$ pairs of the canonical population, the
agreement effect has a mean of $\LedgerMeanThetaD$ with a standard error of $\LedgerMeanThetaDSE$
% [MEAN_THETA_D]
--- a positive average effect, precisely estimated, and roughly a fifth of the
dispersion around it. The quantity is a log change, so the level-scale
counterpart of the average pair is not the exponential of this number: with
dispersion of the order documented below, $\E[\exp(\theta)]$ and
$\exp(\E[\theta])$ differ materially, and the two should not be substituted for
one another. We report effects on the log scale throughout and defer the
level-scale translation, where it is needed, to
Section~\ref{sec:propagation}.

% ¶2 — Trade-weighted mean, with the endogeneity-of-weights sentence required by INV-034.
Weighting pairs by the volume of trade they carry rather than counting them
% [TW_MEAN]
equally drives the average down to $\LedgerTWMean$. The weights are
pre-adoption trade, not contemporaneous or post-adoption trade, and the choice
is not innocuous: post-treatment volume is itself a function of the effect
being estimated, so weighting by it would let large estimated effects earn
% [INV-034]
themselves large weights. On pre-adoption weights, the
dollar-weighted average agreement effect is roughly a third of the
pair-weighted one. That gap is not measurement error. It is the first
appearance of a structural feature that organises the rest of this section:
the effect is not the same size everywhere, and the places where trade is
concentrated are not the places where the effect is large.

% ¶3 — Dispersion as an interval, arm logic in one sentence, cross-ref to the derivation.
The dispersion of true effects is identified only up to an interval. Subtracting
the noise-only null from the observed variance leaves a standard deviation of
$1.475$; subtracting the hardened out-of-sample drift null leaves $0.740$; the
% [SD\_THETA\_TRUE]
identified set is $[\LedgerSDTrueLo,\,\LedgerSDTrueHi]$, and the two endpoints
differ in exactly one assumption --- whether the persistent, pair-specific
component that the placebo machinery reproduces is counted as noise to be
removed or as effect heterogeneity to be retained. The construction is derived
in Section~\ref{sec:empirical-dispersion} and we do not prefer an arm here.
Both endpoints are large relative to the mean. Under either, the standard
deviation of the effect exceeds the average effect by a factor of three or
more, and any statement about ``the'' agreement effect is a statement about the
first moment of a distribution whose second moment dwarfs it.

% ¶4 — The P(theta<=0) bracket, both assumptions in the sentence that reports it.
The probability that a randomly chosen pair's true effect is non-positive lies
% [P\_THETA\_LEQ\_0]
in $[\LedgerPLo,\,\LedgerPHi]$, where the lower endpoint is the
normal-form probability evaluated at the hardened arm and the upper endpoint is
capped at the empirical share of non-positive estimates rather than taken from
the normal form, which at the noise-only arm returns a value exceeding that
% [P\_LO; P\_HI]
share. Both assumptions are load-bearing and neither is
innocent: the first imposes a shape the data do not identify, the second
substitutes an observed frequency for a modelled one. Separately, $42.11$ % [RAW_SHARE]
percent of the pair-level estimates are themselves non-positive, but that figure is a property of the noisy estimates, not of
the effects, and it bounds nothing but itself.

% ¶5 — The honesty paragraph: what the achievable signal share does and does not permit.
What cannot be reported is the shape. At the signal share these data achieve
--- roughly $0.37$ of the observed variance attributable to true effect % [P_LO]
variation --- the mixture deconvolution does not resolve: fitting three
components returns duplicates sitting at the standard-deviation floor rather
% [INV-029]
than distinct subpopulations. This is a limit of the measurement,
and it forecloses a family of claims that the literature makes casually. We
cannot report the share of pairs that are winners, because that requires
locating mass on one side of a threshold in a density we do not have. We cannot
report tail asymmetry, or skewness, or the weight of an upper component, or a
bulk-and-winners decomposition, because each of those is a statement about
shape. We cannot report a point probability of a non-positive effect, which is
why the previous paragraph reports a bracket assembled from two assumptions
rather than a number.

% ¶6 — What the shape limit does NOT foreclose; sets up S5.2 and S5.3.
What survives the limit is more than it might appear. The first moment is
point-identified and precisely estimated. The second is set-identified, with
both endpoints reported and the assumption separating them named. And the
systematic variation in the first moment across observable pair
characteristics --- the subject of the next subsection --- is point-identified
throughout, because it requires only conditional means, not a density. A
distribution can be unknown in shape while its location, its spread to within
an interval, and its dependence on covariates are all recoverable. That is the
position these data put us in, and the rest of this section works within it
rather than around it.

\begin{table}[htbp]
\centering
\caption{The corrected effect distribution}
\label{tab:effect-distribution}
\begin{tabular}{lr}
\toprule
Quantity & Value \\
\midrule
Pairs & $\LedgerN$ \\
Mean effect $\bar\theta_D$ & $\LedgerMeanThetaD$ \\
\quad standard error & $(\LedgerMeanThetaDSE)$ \\
Trade-weighted mean, pre-adoption weights & $\LedgerTWMean$ \\
Standard deviation of estimates & $1.5614$ \\ % [T5R: SD_theta_D]
Standard deviation of true effects, identified set & $[\LedgerSDTrueLo,\,\LedgerSDTrueHi]$ \\
\quad hardened drift arm & $\LedgerSDTrueLo$ \\
\quad noise-only arm & $\LedgerSDTrueHi$ \\
$\Pr(\theta \leq 0)$, bracket & $[\LedgerPLo,\,\LedgerPHi]$ \\
Share of estimates $\leq 0$ & $\LedgerRawShare$ \\
Signal share & $0.37$ \\ % [P_LO]
\bottomrule
\end{tabular}
\end{table}

\subsection{The Size Gradient}
\label{sec:results-gradient}

% ¶7 — The gradient itself: fall-then-floor, with the SEs that make the shape claim.
Sorting pairs into quintiles of pre-adoption trade volume produces the sharpest
structural fact in the data. The profile is reported in
Table~\ref{tab:gradient}. It runs from $\LedgerGradientQOne$ in the smallest quintile to
% [GRADIENT\_Q1; GRADIENT\_Q5]
$\LedgerGradientQFive$ in the largest, a spread of $\LedgerGradient$
% [GRADIENT]
with a standard error of $\LedgerGradientSE$ --- eleven standard errors from
zero. Two features of its shape carry the argument. The decline is steep across
the first three quintiles and then stops: the fourth and fifth quintiles differ
by $-0.008$ against a standard error of $0.046$, which is to say they cannot be % [T27_gradient_B: Q4-Q5]
told apart. And the floor they share is small but not zero, the largest
quintile differing from zero at the five percent level where the fourth does
not.

% ¶8 — Why fall-then-floor is the predicted shape, not a weaker version of monotonicity.
The shape matters as much as the slope. Section~\ref{sec:theory-nonsep}
predicts effects that decline in prior integration and then settle at a common
floor once the constraints an agreement removes have stopped binding, and it is
explicit that a smoothly monotone decline continuing through the largest pairs
would count against the mechanism rather than for it: there is no reason for
integration to keep reducing an effect that has already been exhausted. The
observed profile is a fall and then a floor. The floor is small and slightly
negative rather than exactly zero, which the mechanism does not forbid --- what
it forbids is continued decline --- and the two quintiles that constitute it
cannot be told apart. This is a more specific prediction than a negative slope,
and it is the one the data deliver.

% ¶9 — Economic reading, tied to the theory's graded-effects prediction.
The economic content is straightforward. An agreement removes a set of
constraints, and the gain from removing them scales with how tightly they bind.
Pairs in the smallest quintile trade an average of well under a million dollars
before adoption; pairs in the largest average more than fifteen billion
(Table~\ref{tab:gradient}). Four orders of magnitude separate them,
and they are not the same economic object. For a pair already trading heavily,
the frictions an agreement addresses are, by revealed behaviour, not the
frictions that were preventing trade; whatever was binding has already been
worked around, absorbed into distribution networks, or negotiated away
bilaterally. For a pair trading almost nothing, the same removed constraint can
be the difference between a market that exists and one that does not. The
gradient is what graded effects look like when the grading variable is prior
integration.

% ¶10 — Robustness: the gradient predates the correction layer.
The gradient is not manufactured by the correction. Computing the same
quintile profile on the uncorrected pair-level estimates --- before the
placebo-based bias correction is applied, reported alongside the corrected
profile in Table~\ref{tab:gradient} --- returns a smallest-to-largest spread of
% [GRADIENT\_B]
$0.683$ with a standard error of $0.082$, which is
% [GRADIENT\_B\_SHARE]
$74.7$ percent of the corrected spread. Three quarters
of the gradient is present in the raw estimates. The correction sharpens it and
shifts its level, as it shifts every level in this paper, but it does not
create it. A reader who distrusts the correction machinery entirely still faces
a size gradient of roughly seven tenths of a log point across the trade
distribution.

\begin{table}[htbp]
\centering
\caption{Agreement effects by pre-adoption trade quintile}
\label{tab:gradient}
\begin{tabular}{lrrrrr}
\toprule
 & \multicolumn{2}{c}{Corrected ($D$)} & \multicolumn{2}{c}{Uncorrected ($B$)} & Pre-adoption \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Quintile & Mean & (SE) & Mean & (SE) & trade (\$m) \\ % [T27_gradient_B]
\midrule
1 (smallest) & $0.855$ & $(0.076)$ & $0.557$ & $(0.077)$ & $0.7$ \\
2            & $0.374$ & $(0.060)$ & $0.137$ & $(0.062)$ & $7.8$ \\ % [T27_gradient_B]
3            & $0.131$ & $(0.049)$ & $-0.022$ & $(0.050)$ & $47.1$ \\
4            & $-0.066$ & $(0.037)$ & $-0.168$ & $(0.038)$ & $251.6$ \\ % [T27_gradient_B]
5 (largest)  & $-0.058$ & $(0.028)$ & $-0.126$ & $(0.028)$ & $15{,}691.0$ \\
\midrule
Spread, 1 $-$ 5 & $0.914$ & $(0.081)$ & $0.683$ & $(0.082)$ & \\ % [T27_gradient_B_spread]
\bottomrule
\end{tabular}
\end{table}

\subsection{The Specification Spread, Resolved}
\label{sec:results-spread}

% ¶11 — Restate the puzzle from the introduction, now with the four numbers.
The puzzle set out in Section~\ref{sec:introduction} can now be answered. Four
specifications of the same gravity equation, on the same data, estimating the
same nominal parameter, return agreement effects of $\LedgerSpecB$, $\LedgerSpecC$, $\LedgerSpecCY$
% [T1R\_spec\_spread]
and $\LedgerSpecFull$. The spread is a factor of fifteen. Each of
the four is defensible by some standard in the literature, and the discipline
has largely treated the range as a robustness exercise --- a band within which
the truth presumably lies, to be narrowed by better controls.

% ¶12 — Dismiss (i) and (ii) in one sentence, as the structure requires.
Two of the four can be set aside quickly. The first and second omit
multilateral resistance terms, and their deviation from the remainder is bias
of a kind the literature settled decades ago; nothing in this paper is required
to explain why they are wrong.

% ¶13 — The genuine puzzle: (iii) vs (iv), both defensible.
The genuine puzzle is the third against the fourth: $\LedgerSpecCY$ from a
country-year fixed-effects specification and $\LedgerSpecFull$ once pair fixed effects
are added. Both handle multilateral resistance. Both are standard. The second
is the more conservative and is frequently described as such, on the reasoning
that identifying from within-pair variation strips out whatever is fixed about
a pair and leaves the agreement's own contribution. On that reasoning the
smaller number is the more credible one, and the discipline has broadly
accepted the ordering.

% ¶14 — The resolution: differently-weighted averages over one sloped distribution.
The results above supply a different reading. If the effect were a constant,
every consistent estimator would recover it and the spread would be sampling
noise. The effect is not a constant; it is a distribution with a strong slope
in pair size. Every estimator therefore reports an average over that
distribution, and which average it reports is determined by its implicit
weights. A specification whose identifying variation is dominated by large
trade flows samples the flat end of the gradient, where effects are near the
floor. A pair-fixed-effects specification identifies only from switchers, on a
subpopulation defined by the exclusion rules set out in
Section~\ref{sec:data-population}, and weights them non-convexly. Neither is
estimating the other's parameter incorrectly. They are estimating different
weighted averages of the same sloped object, and the ordering between them
carries no presumption about credibility.

% ¶15 — Anchor in the paper's own numbers, with the honest limit on what is identified.
The paper's own estimates trace the same pattern. The dollar-weighted mean is
% [TW\_MEAN]
$\LedgerTWMean$, close to the fourth specification's $\LedgerSpecFull$; the
% [MEAN\_THETA\_D]
unweighted pair-level mean is $\LedgerMeanThetaD$; the country-year
specification returns $\LedgerSpecCY$. Moving from counting pairs to counting dollars
moves the average by roughly the same distance as moving between the two
defensible specifications. We state plainly what this does and does not
establish. It establishes that weighting alone generates spread of the observed
magnitude on a distribution with this slope. It does not establish that
weighting is the whole of the wedge between the pair-level means and the
cross-sectional coefficient: that wedge also contains selection on match
quality, since the pairs that sign agreements are not drawn at random from the
pairs that could, and this design does not separately identify the two
channels. What can be said is the weaker and sufficient claim. The coefficient
any one specification reports is an average over a slope, addressed to no
particular pair.

\subsection{What This Confirms and What It Leaves Open}
\label{sec:results-reconciliation}

% ¶16 — Predictions (i) and (iii): confirmed.
Section~\ref{sec:theory} advanced four predictions and asked that
they be held jointly. Two now carry evidence, and the reason the third does not
is worth stating rather than eliding. Lognormal-type outcome tails were to be
tested in the distributional diagnostics; no such diagnostic is reported, and the
prediction is left untested. What Section~\ref{sec:empirical} does test is the
narrower distributional assumption the propositions themselves maintain --- that
the within-pair log gaps are normal --- and that assumption is rejected, with the
departure running to the left rather than the right.
% [A2_REJECT_REAL, A2_REJECT_CONTROL, A2_SKEW]
 The mechanism's tail prediction therefore stands neither confirmed nor
refuted, and the reader should discount the conjunction accordingly. Effects
graded in size and prior integration are confirmed here, and confirmed in the
specific fall-then-floor form the mechanism requires rather than merely as a
negative slope.

% ¶17 — Prediction (iv) supported; prediction (ii) under-identified, and what that means.
Selection on match quality is supported rather than confirmed, on the placebo
and stability evidence assembled in Section~\ref{sec:empirical-diagnostics}:
the pairs that adopt are systematically unlike the pairs that do not, in ways
that persist and that the correction layer detects but does not fully purge.
Right-skewed effects are neither confirmed nor refuted. The prediction is
under-identified at the signal share these data achieve, for the reason given
above, and we decline to report the skewness statistics that would appear to
settle it. This is the one point in the paper where a prediction that could
have been tested is not, and it should be read as a measurement limit rather
than as a null result: nothing here weighs against right-skewed effects, and
nothing here weighs for them.

% ¶18 — The conjunction, stated exactly: two legs carry weight, and they are
% not worth the same. Matches the advance scorecard in sec:theory-standard.
The abductive argument therefore stands on two legs that carry weight, one that
is under-identified, and one that is untested, and the two that carry are not
worth the same. Section~\ref{sec:theory-standard} rested the case on the
conjunction of all four because each is weak alone, and half of that conjunction
has gone untested here. But the tested half contains the prediction that section
named as the sharpest of the four: the one that specifies a direction and a shape
against variables the data contain, and that nominates its own falsifier in a
smoothly monotone descent. That falsifier was available. Nothing in the estimator
imposes a floor, the quintile means are unrestricted, and the profile was free to
continue down through the largest pairs. It does not. Set against the
alternatives conceded for this prediction --- diminishing returns of a wholly
different origin, or measurement error correlated with volume --- what those
deliver is a negative slope, and what the data show is a fall arrested at a
common floor, which none of them predicts as stated. The gradient is therefore
discriminating evidence, and it is the only discriminating evidence the paper
has: selection on match quality is consistent with almost any account of why
governments negotiate, and corroborates without separating. We claim accordingly.
The mechanism survived a test it could have failed, on the prediction it staked
most on, and the two predictions that would have widened the separation remain
open --- right-skewed effects because the shape is under-identified at this
signal share, lognormal-type outcome tails because the prediction concerns the
marginal distribution of flows and this design reaches it only through the
normality assumption the propositions maintain, which
Section~\ref{sec:empirical} rejects in a direction that favors neither reading.
Neither is recoverable from these data, and we claim neither.

% =============================================================================
% D2 — TABLE NOTES
% =============================================================================
% Short captions are live in the table environments above. The following notes
% belong beneath each table; they are held here rather than inline so the
% caption stays scannable.
%
% Table~\ref{tab:effect-distribution}. Canonical population of 4,182 country
% pairs adopting an agreement between 1992 and 2015, each observed over a
% symmetric pre- and post-adoption window. theta_D is the pair-level log effect
% after bias correction against a never-treated placebo population with
% pseudo-adoption dates. The mean and its standard error are point-identified.
% The standard deviation of true effects is an identified set whose endpoints
% differ in whether the persistent pair-specific component reproduced by the
% placebo machinery is treated as noise or as effect heterogeneity; no arm is
% preferred. Pr(theta <= 0) is a bracket, not an estimate: the lower endpoint
% imposes a normal form at the hardened arm, the upper is capped at the
% empirical share of non-positive estimates. THE SHAPE OF THE EFFECT
% DISTRIBUTION IS NOT IDENTIFIED at this signal share; mixture deconvolution
% returns duplicate components at the standard-deviation floor (INV-029). No
% quantity in the table depends on a distributional assumption about the
% effects except where stated.
%
% Table~\ref{tab:gradient}. Quintiles are formed on pre-adoption bilateral
% trade volume over the canonical population (n = 837, 836, 836, 836, 837).
% Definition D is the bias-corrected pair-level effect; Definition B is the
% same quantity before correction, reported so the gradient can be assessed
% independently of the correction layer. Standard errors are within-quintile
% standard deviations divided by the root of the quintile count; the spread's
% standard error combines the two endpoint standard errors. THE PROFILE IS NOT
% MONOTONE: quintiles 4 and 5 are statistically indistinguishable from one
% another, and the mechanism of Section 2 predicts a floor rather than
% continued decline. Pre-adoption trade is the within-quintile mean in millions
% of US dollars.

% =============================================================================
% D3 — RECONCILIATION NOTE (verbatim reuse in the theory section and the coda)
% =============================================================================

% CORRECTED 2026-08-06. The prior version of this note stated that lognormal-type
% outcome tails are "confirmed by the distributional diagnostics" and that the
% conjunction stands on three legs. Both are false against ¶16 of this section
% and against the advance scorecard in Section~\ref{sec:theory-standard}. Do not
% reuse any earlier copy of this note.
%
% Of the four predictions advanced in Section~\ref{sec:theory-distribution}, two
% carry weight, one is under-identified, and one is untested, and the two that
% carry are not worth the same. Effects graded in size and prior integration are
% confirmed in Section~\ref{sec:results}, in the fall-then-floor form the
% mechanism requires rather than as a bare negative slope. This is the prediction
% Section~\ref{sec:theory-standard} named as the sharpest of the four; it
% nominates a smoothly monotone descent as its own falsifier; the quintile means
% are unrestricted and the falsifier did not appear. It is the paper's one piece
% of discriminating evidence. Selection on match quality is supported --- not
% confirmed --- by the placebo and stability evidence of
% Section~\ref{sec:empirical-diagnostics}; it corroborates without separating,
% being consistent with almost any account of why governments negotiate.
% Right-skewed effects are neither confirmed nor refuted: the shape of the effect
% distribution is not identified at the signal share these data achieve, and that
% is a measurement limit rather than a null result. Lognormal-type outcome tails
% are not tested here; the prediction concerns the marginal distribution of
% flows, and this design reaches it only through the normality assumption the
% propositions maintain, which Section~\ref{sec:empirical} rejects in a direction
% that favors neither reading. The conjunction on which the case was rested is
% therefore half assembled, and what the assembled half establishes is that the
% mechanism survived the one test it could have failed.

% =============================================================================
% D4 — AMBIGUITIES (unresolved)
% =============================================================================
%
% A1. [CLOSED 2026-08-10] S5.4 was specified to state that the graded-effects
%     finding is "larger than any prior literature reports." No ledgered source
%     supports a comparison to prior estimates, and none is sought: the
%     literature reports single coefficients rather than pair-level gradients,
%     so there is no comparable quantity to be larger than. The clause is
%     dropped rather than deferred. The contribution does not rest on the
%     gradient exceeding a prior estimate; it rests on there being a gradient
%     where the literature reports a point.
%
% A2. [SUPERSEDED VALUE] The drafting specification froze TW_MEAN at 0.0897.
%     The ledger records 0.0898 (raw 0.0897566731583432 at four decimal
%     places); the earlier figure truncated rather than rounded. 0.0898 is used
%     throughout. See canonical_facts.md, Effect Distribution note.
%
% A3. [SPECIFICATION CONTRADICTED BY DATA] The specification required S5.2 to
%     state that the effect "declines monotonically" and that the largest
%     pairs' effects are "statistically indistinguishable from zero." Both fail
%     against T27_gradient_B.csv: Q5 (-0.0583) lies above Q4 (-0.0664), and Q5
%     differs from zero at t = -2.11 while Q4 does not (t = -1.80). The section
%     reports fall-then-floor, per the binding shape note in the ledger's
%     Gradient section. This is the stronger reading: Section 2 states that a
%     smoothly monotone negative profile would falsify the mechanism.
%
% A4. [SOURCE SUBSTITUTION] The specification named T12_N4_gradient as the
%     gradient source. That file is registered SUPERSEDED (SYNC-4). All
%     gradient quantities are cited from the ledger, whose producer is
%     S28_gradient_B.R -> T27_gradient_B*.csv; T27 reproduces every
%     T12_N4_gradient column byte-identically under gates G3-G5.
%
% A5. [SOURCE RESTRICTION] T10R_reconciliation.csv, a permitted source, carries
%     three forbidden values in its `pack` column (0.304, 0.214, 0.465). Only
%     the `fixed` column is cited. No quantity in this section is sourced from
%     T10R directly.
%
% A6. [PLACEHOLDER RESOLVED] The specification's [NEEDED: gradient share
%     attributable to theta_B] is closed. GRADIENT_B = 0.6827 (SE 0.0821) and
%     GRADIENT_B_SHARE = 0.7472 are ledgered as of SYNC-7.
%
% A7. [STRUCTURAL DEVIATION] S5.3 was specified to open by restating the
%     introduction's puzzle by cross-reference. The four specification
%     estimates do not currently appear in Section 1; the puzzle is restated in
%     words with a cross-reference to sec:introduction, and the four numbers
%     appear here for the first time. If Section 1 is later revised to state
%     them, ¶11 should be trimmed accordingly.
%
% A8. [CLOSED 2026-08-06] ¶1 previously deferred level-scale translation to
%     "Section \ref{sec:results}'s successor" because sec:propagation did not
%     yet exist. Section 7 is now committed and labelled; ¶1 cites
%     \ref{sec:propagation} directly. sec:conclusion is defined by the Section 8
%     coda as of 2026-08-06; the forward references to it now resolve.
%
% A9. [CLOSED 2026-08-10] ¶14 cites sec:data-population for the switcher
%     exclusion rules and the non-convex weighting. The switcher material is
%     located there (main.tex lines 492-530). The projection algebra referred
%     to in the drafting specification was not located under any label and is
%     presumed absent from the current draft; no reference move is required.
%
% A10. [CLOSED 2026-08-06] The T9 and T22 placeholders are resolved as
%     tab:holdout and tab:reliability. The threshold-grid placeholder is
%     removed rather than filled: the robustness claim it pointed to is
%     withdrawn from Section 3, because output/T30_threshold_grid.csv has not
%     been independently re-derived. The artifact and its producer remain in
%     the tree, registered and uncited. No placeholder of that form remains.
%
% A11. [CLOSED 2026-08-10] Body prose is approximately 1,770 words, below the
%     specified 2,400-3,200 band. This shortfall is accepted: expanding A1
%     (the causal-mechanism discussion) or the honesty paragraph requires
%     material the current fact sources do not contain, and padding with
%     filler would violate the specification's parsimony guidance.
%
% A12. [TABLES] Both exhibits are now written inline with live labels
%     (tab:effect-distribution, tab:gradient) and are cited at paragraphs 1, 7,
%     9 and 10. Sources: the ledger for the distribution summary,
%     output/T27_gradient_B.csv for every gradient cell. booktabs is loaded in
%     the preamble; no siunitx, so alignment is plain r-columns.




% =============================================================================
% SECTION 6 — THE BOUNDARY OF LEARNABILITY
% Fact convention: trailing "% [ID]" per quantitative claim.
% =============================================================================

\section{\label{sec:limits}The Boundary of Learnability}

\subsection{\label{sec:limits-equivalence}The Equivalence Theorem}

The sharpest limit these data impose is not a matter of statistical power, and no
larger sample relieves it. Within the post-adoption window, transitory
pair-level variation in the effect and a treatment-coincident change in the
volatility of trade itself enter the estimating problem only through their sum.
Proposition~\ref{prop:ident}(b) states this exactly: conditional on the pair, the
post-adoption log gaps are independent and identically distributed as
$N(\theta_{ij},\,\sigma^2_{ij}\rho^2+\omega^2)$, so transitory effect variation
and the post-to-pre volatility ratio reach the likelihood only through the
composite, and any two parameterizations sharing that composite generate
identical data distributions.
% [Prop3]
 Distributions that are identical cannot be separated by any statistic computed
from them.

The content of that statement is easiest to see as a pair of economies. In the
first, the bite of an agreement flickers within the pair: its persistent
component is fixed, and a transitory increment of variance is drawn afresh each
post-adoption year. In the second, the bite is perfectly steady and what
treatment changes is the environment, scaling each pair's pre-adoption dispersion
by a common factor. These are different economic claims with different content.
Choose the two parameters so that the composites agree, and no statistic
distinguishes the economies.

The consequence is the thesis of this section. Under the model's normality
assumption, ``heterogeneous effects'' and ``noisier outcomes'' are labels for the
same data, and the choice between them is a modeling commitment rather than an
estimate. Two qualifications follow immediately, and both are material. The
complement deserves equal emphasis: Proposition~\ref{prop:ident}(a) identifies
the persistent component, the interval this paper reports is an interval in that
quantity, and its width is generated by the noise-attribution arms rather than by
the equivalence of part~(b).
% [SD_THETA_TRUE]
 And the equivalence is exact only under normality. Outside it, the composite no
longer exhausts the likelihood, and Remark~\ref{rem:moments} reports what a
pooled higher-order moment recovers at this sample: separation of
$\MomPowMinZ$ to $\MomPowMaxZ$ under cross-pair independence, against a measured
departure that falls inside the simulated range.
% [MOMPOW_IDENT_MIN_Z, MOMPOW_IDENT_MAX_Z, A2_EXKURT]
 The boundary is therefore measured rather than absolute, and its position
depends on two conditions --- cross-pair independence and a shock shape treatment
does not alter --- neither of which this paper establishes.

\subsection{\label{sec:limits-warning}A Warning for Practice}

Corollary~\ref{cor:rhoop} generalizes what
Section~\ref{sec:empirical-certificate} demonstrated on a single estimator.
Define the operational dispersion ratio as a pair's post-window variance over its
pre-window variance. Under the model of Proposition~\ref{prop:ident} that ratio
carries the transitory effect variation inside it, indistinguishable from a
volatility change, exactly as part~(b) requires. A null calibrated by scaling a
pre-based noise variance of shorter window by that ratio therefore subtracts the
effect variation along with the noise, and over-subtracts by a derivable window
factor; at this paper's calibration the whole of the estimated heterogeneity is
absorbed at $\kappa_{\mathrm{floor}}=\PropKappaFloor$.
% [Cor:rhoop]
 Two properties of this class of estimator are worth stating plainly. The bias
is signed and computable rather than a matter of luck. And it is largest where
the quantity of interest is largest, since the term inflating the operational
ratio is the transitory variance itself: an estimator of this class returns its
most reassuring number --- heterogeneity near zero --- in precisely those worlds
where transitory heterogeneity is greatest.

\subsection{\label{sec:limits-tolerances}Bounded Tolerances}

Two further limits are bounds rather than walls, and each carries its number. The
first is the resolution of the drift calibration. The correction is estimated on
held-out placebo pairs and validated on them decile by decile against a bound of
$0.10$ log points; nine of the ten size deciles clear it, and clear it with room, % [GATE_DECILE_BOUND]
the largest absolute mean among those nine being $\HoldoutMaxAbsExDThree$. The
third decile returns $\HoldoutDThreeMean$ on $\HoldoutDThreeN$ validation pairs
and registers PARTIAL.
% [HOLDOUT_D3_MEAN, HOLDOUT_D3_N, HOLDOUT_MAX_ABS_EX_D3]
 That bound is the resolution of the correction layer, and it governs what may
be claimed about fine structure: variation in the size profile smaller than about
a tenth of a log point lies inside the calibration tolerance, and this paper
makes no claim at that resolution. The fall-then-floor reading of
Section~\ref{sec:results-gradient} does not require any. It turns on the collapse
from the smallest quintile to the floor, which exceeds the tolerance by nearly an
order of magnitude, and it asserts nothing about the ordering of the quintiles
constituting the floor.

The second is the distributional assumption itself. The propositions are derived
under normality of the within-pair log gaps, and that assumption is tested
directly in Section~\ref{sec:empirical} and rejected, at $32.8$ percent of pairs % [A2_REJECT_REAL, A2_REJECT_CONTROL]
against $5.0$ percent for a matched control.
 Two things follow, and they pull in opposite directions. The predictions the
propositions generate survive the failure of their own premise --- the
reliability formula lands within eight thousandths of the observed value on the
same data
% [R_PRED, PLACEBO_A_R]
--- which is evidence about the mechanism rather than about the premise. But the
equivalence of Section~\ref{sec:limits-equivalence} was exact only under that
premise, and its failure is what opens the higher-moment route. The paper reports
both rather than choosing.

\subsection{\label{sec:limits-transport}The Transportability Boundary}

The estimand is a distribution of effects over self-selected matches, and that is
a limit of the same standing as the equivalence theorem: proven by the design
rather than conjectured about it. The estimating population consists of pairs
that adopted an agreement, and the counterfactual against which their effects are
measured is fitted on untreated cells only
(Section~\ref{sec:data-comparison}). Nothing in that construction identifies the
effect of an agreement assigned to a pair that did not choose one. No such pair
appears in the estimating population, and no assumption available to the design
manufactures one. This is stronger than the customary caution about external
validity, which concerns whether an estimate travels: here the population over
which the estimand is defined does not contain the pairs to which a reader might
wish to extrapolate.

The margin at issue is the one the theory names in advance.
Section~\ref{sec:theory-nonsep} predicts that agreements are selected on the same
unobserved conditions that modulate their effects, and the placebo and stability
evidence of Section~\ref{sec:empirical-diagnostics} supports that prediction. The
prediction and the limit are one fact seen from two sides: the selection that
makes the mechanism testable is the selection the estimand conditions on. That
conditioning is the constraint any policy reading must respect.

\subsection*{The identification ledger}

\begin{table}[htbp]
\centering
\caption{The identification ledger}
\label{tab:identification}
\begin{tabular}{lll}
\toprule
Quantity & Status & Instrument \\
\midrule
Dispersion of persistent effects & Set-identified & Prop.~\ref{prop:ident}(a); arm accounting \\
Transitory/volatility split & Conditionally unidentified & Prop.~\ref{prop:ident}(b); Rem.~\ref{rem:moments} \\
Drift-calibration residual & Bounded at $0.10$ & Out-of-sample decile validation \\ % [GATE_DECILE_BOUND]
Effect on a non-adopting pair & Unidentified by design & Estimating-population construction \\
\bottomrule
\end{tabular}
\end{table}

\noindent Each row records a quantity this design bears on, its status, and the
instrument establishing that status. ``Set-identified'' means an interval is
recovered whose endpoints differ in an assumption the data do not adjudicate.
``Conditionally unidentified'' means exactly unidentified under the model's
normality assumption and recoverable outside it only under conditions this paper
does not establish. ``Bounded'' means measured up to a tolerance a gate on
held-out data certifies. No row reports a status stronger than its instrument
supports.

None of these four limits is assumed. The first is a theorem, the second a
corollary of it, the third the reported outcome of a gate run on held-out data,
and the fourth a property of a population whose construction is tabulated. Each
also names its own exit. The equivalence is broken by exogenous variation in
outcome volatility, or by non-normality sufficient to make pooled higher-order
moments informative --- a route this paper measures but does not walk. The
over-subtracting null is defeated by matching the null's window to the estimation
window, which is a design choice rather than a discovery. The calibration
tolerance narrows with cells that are finer and still large enough to certify.
The transportability boundary is crossed only by variation in assignment. What
remains between the limits is the shape of this paper's contribution: the
diagnostics that could be computed were confidently wrong, the quantities that
mattered most are bounded rather than free, and the section between those two
facts is where this paper's positive claims live.


% =============================================================================
% SECTION 7 — PROPAGATION AND IMPLICATIONS
% Sources: meta/canonical_facts.md (via article/ledger_constants.tex macros);
%          output/T20_ge_bracket.csv  (partial-equilibrium distributional bracket)
%          output/T42_ge_twodyad.csv  (conditional GE, common mean, two dyads)
%          output/T43_ge_gradient.csv (conditional GE, quintile-conditional means)
% All ledgered quantities are cited by macro. Table cells are cited by
% % [Txx: row, col] comments on their own lines.
% =============================================================================

\section{Propagation and Implications}
\label{sec:propagation}

\subsection{From Parameters to Outcomes}
\label{sec:prop-design}

% ¶1 — What the translation needs before it can run.
Everything reported to this point is a log effect. A reader who wants to know
what an agreement does to trade needs that quantity on the scale trade is
measured in, and the translation is not a change of units. It requires three
inputs the previous sections supply separately: a scale map, which is the
exponential; a dispersion, which this design identifies only up to an interval;
and, wherever the question concerns a quantile rather than a location, a shape,
which at the signal share these data achieve is not identified at all
(Section~\ref{sec:results}). This section performs the translation with each of
those inputs named where it enters, and reports the result as a bracket rather
than a number.

% ¶2 — Two propagation designs, and what each can and cannot answer.
Two exercises are reported, and they answer different questions. The first is
distributional and partial: it maps the estimated effect distribution onto the
level scale pair by pair, holding multilateral resistances, third-country
reallocation, incomes and expenditures fixed. It says what the spread of
outcomes looks like across the population of adopters, and nothing about
equilibrium adjustment. The second is a conditional general-equilibrium
counterfactual: a structural gravity system solved on the terminal
cross-section, with an elasticity of substitution of five taken as the
literature standard, incomes and expenditures held at their data values, and
prices and multilateral resistances allowed to adjust
\citep{anderson2003gravity}. Because incomes are fixed, every quantity it
returns is a change in bilateral trade, not in welfare, and it is reported as
such throughout.

% ¶3 — The calibrated dyads, and why two.
The equilibrium exercise must be evaluated at a particular pair, and the choice
of pair is itself a modeling decision, so two are used rather than one. Both are
drawn from the adopting population and from opposite ends of the pre-adoption
trade distribution: the median pair of the smallest quintile and the median pair
of the largest, on the same quintile partition that generates the size gradient
in Section~\ref{sec:results-gradient}. Reporting both, separately, is what
allows the equilibrium response to be distinguished from the parameter it
propagates --- a distinction the next subsections turn on.

% ¶4 — The imposed form, stated as an imposition.
Where a quantile is required and the shape is unidentified, a form must be
imposed. The form imposed here is a symmetric normal with the ledgered mean and
the arm's standard deviation, and both exercises are run at both endpoints of
the identified set for the dispersion of true effects,
$[\LedgerSDTrueLo,\,\LedgerSDTrueHi]$, with no endpoint preferred. Symmetry is
an assumption of convenience where the data are silent, not a finding, and
nothing below should be read as evidence about the shape of the effect
distribution.

% ¶5 — The governing sentence, before any number.
One sentence governs every figure in this section. Each outcome range below is
doubly conditional --- on which admissible null model sets the dispersion arm,
and on the symmetric form imposed where the shape is unidentified --- and
neither condition is a technicality the reader may discharge and forget. The
first is set-identified by construction; the second is the measurement limit
stated in Section~\ref{sec:results} and formalised in Section~\ref{sec:limits}.
What follows are brackets of brackets. A figure lifted out of one and quoted
alone will be wrong in a direction the lifter cannot sign.

\subsection{The Typical Pair and the Typical Dollar}
\label{sec:prop-typical}

% ¶6 — The mean effect propagated, with the Jensen caveat Section 5 deferred here.
The mean agreement effect is $\LedgerMeanThetaD$ with a standard error of
$\LedgerMeanThetaDSE$, which exponentiates to a trade increase of
$\LedgerMeanThetaDPct$ percent. The label on that number requires the care
Section~\ref{sec:results} flagged and deferred. Exponentiating the mean of a log
effect returns the level change for a pair at the centre of the log-effect
distribution --- under the symmetric form imposed here, the median pair --- and
not the average level change across pairs. The latter is larger by a Jensen term
in the dispersion, and since the dispersion is set-identified the average level
change is set-identified too and is not ledgered. The central pair gains about
$\LedgerMeanThetaDPct$ percent. The average pair gains more, by an amount this
design does not pin down.

% ¶7 — The dollar-weighted counterpart and the endogeneity-of-weights clause.
Weighting by trade rather than by pair, the same quantity is $\LedgerTWMean$, or
$\LedgerTWMeanPct$ percent. The weights are pre-adoption trade, for the reason
given in Section~\ref{sec:results}: post-adoption volume is a function of the
effect being weighted, so weighting by it would let large effects purchase their
own importance. On honest weights the dollar-weighted answer is roughly a third
of the pair-weighted one.

% ¶8 — Why two audiences get answers three times apart.
Two audiences therefore receive answers differing by a factor of about three,
and the gap is not a disagreement about the data. A trade ministry asking what
an agreement will do for a country pair is asking a pair-weighted question; a
finance ministry asking what a programme of agreements will do to aggregate
trade is asking a dollar-weighted one. Both are answered correctly from the same
distribution, and they differ because the effect is systematically smaller where
the dollars are.

% ¶9 — What the gap is not.
It is worth stating what that gap is not. It is not a bias in either figure, and
it is not resolved by a longer panel or a different fixed-effect structure. Both
averages come from the same pair-level estimates under the same correction and
differ only in the weight attached to each pair. Any weighting scheme is an
implicit choice of whose question is being answered, and on a distribution with
a slope this steep the choice is consequential. A literature reporting one
number is not reporting a compromise between the two; it is reporting one of
them, usually without saying which.

\subsection{Size-Conditional Expectations}
\label{sec:prop-size}

% ¶10 — The gradient, propagated.
Sorted into quintiles of pre-adoption trade, the effect runs from
$\LedgerGradientQOne$ with a standard error of $\LedgerGradientQOneSE$ in the
smallest quintile to $\LedgerGradientQFive$ with a standard error of
$\LedgerGradientQFiveSE$ in the largest. On the level scale that is a central
expectation of a $\LedgerGradientQOnePct$ percent increase for a pair in the
smallest quintile and a $\LedgerGradientQFivePct$ percent change for a pair in
the largest. The largest-quintile figure is small, negative, and not zero: it
lies two standard errors below zero.
% [ledger shape note: t = -2.11]
It should be reported as what it is --- a small, precisely estimated, negative
central expectation --- rather than rounded rhetorically to nothing in either
direction. The spread between the endpoints is $\LedgerGradient$ with a standard
error of $\LedgerGradientSE$, and Section~\ref{sec:results-gradient} establishes
that the profile falls and then floors rather than declining throughout.

% ¶11 — The equilibrium exercise separates transmission from parameter.
Two propagations at the same two dyads separate the equilibrium response from
the parameter it carries. Given a common effect distribution, the smallest- and
largest-quintile calibrations return almost the same proportional outcome: the
central expectation differs by less than a percentage point between them, and
the ninety-to-ten ratio by less than one part in a hundred.
% [T42: lo arm, both dyads, q50 and RANGE_1090]
Both pairs are negligible shares of their countries' expenditure, so
multilateral resistance barely moves in either case, and equilibrium
transmission is effectively flat across the size distribution. The consequence
is worth stating plainly: the size gradient in outcomes is a gradient in the
effect parameter, not in the equilibrium response to it.

% ¶12 — The gradient propagated through the equilibrium system.
Propagating each quintile's own central effect through the same system, the
smallest-quintile calibration returns a median trade change of $141$ percent and
the largest-quintile calibration one of $-3$ percent at the hardened arm.
% [T43: lo, LUX_ATG, q50; lo, NLD_CRI, q50]
Equilibrium adjustment moves both toward the middle relative to the partial
figures above, and moves neither far. The gradient survives the general
equilibrium intact.

% ¶13 — The consequence for practice.
The consequence for anyone consulting an estimated agreement effect is direct.
The unconditional coefficient is an average over this slope, addressed to no
particular pair, and the single most decision-relevant conditioning variable is
one every negotiator possesses before any estimation is done: how much the two
countries currently trade with each other. A coefficient reported without that
conditioning is not wrong so much as unaddressed. It answers a question about a
population average that no individual party to an agreement occupies, and at the
endpoints of the trade distribution the distance between that average and the
party's own size-conditional expectation exceeds the average itself.

% ¶14 — What conditioning does not buy.
Conditioning on size sharpens the location of the expectation and not the
uncertainty around it. The standard errors above are standard errors of quintile
means; they describe how precisely the conditional average is estimated, not how
widely true effects are spread within a quintile. That residual dispersion --- 
the quantity a pair would want, having established which quintile it occupies
--- is not separately ledgered, and this paper does not report it. Nothing in
the gradient licenses the inference that a given small pair will gain
$\LedgerGradientQOnePct$ percent. What it licenses is that small pairs gain, on
average, a great deal more than large ones, by a margin many times their common
standard errors.

\subsection{Uncertainty, Stated Honestly}
\label{sec:prop-uncertainty}

% ¶15 — The arm-indexed distributional ranges.
Table~\ref{tab:propagation} reports the outcome distribution implied by each
dispersion arm under the imposed symmetric normal form. At the hardened arm the
tenth percentile of the implied trade change is $-50.1$ percent, the median % [T20: C_hardened, q10]
$28.2$ percent and the ninetieth $231.1$ percent, a ninety-to-ten ratio of % [T20: C_hardened, q50; C_hardened, q90]
$6.64$. % [T20: C_hardened, RANGE_1090]
At the noise-only arm the same three quantiles under the same form are $-81.2$ % [T20: A_noise_only, q10]
percent, $23.5$ percent and $737.2$ percent, a ratio of $44.44$. % [T20: A_noise_only, q50; A_noise_only, q90; A_noise_only, RANGE_1090]
The two ratios differ by a factor of nearly seven, and the difference is not
evidence about the world. It is the price of an assumption the data do not
adjudicate, displayed rather than hidden.

% ¶16 — How to read the width of the arm bracket.
That width should be read as information rather than noise. What separates the
arms is a single substantive question --- whether the persistent, pair-specific
component the placebo machinery reproduces belongs to the effect or to the error
--- and the propagated consequence of answering it one way rather than the other
is a ninetieth percentile of $231$ percent against one of $737$ percent. A
design resolving that question would collapse the bracket to one column.
Section~\ref{sec:limits} explains why the resolution is unavailable from data of
this length rather than of this size; the bracket is not a confidence statement
and does not narrow with more pairs.

% ¶17 — The mechanical asymmetry, with the mechanism named.
Within either arm the upside spread exceeds the downside. The distance from the
median to the ninetieth percentile is $2.59$ times the distance from the tenth % [derived: T20 (q90-q50)/(q50-q10) C_hardened]
to the median at the hardened arm and $6.82$ times it at the noise-only arm. % [derived: T20 (q90-q50)/(q50-q10) A_noise_only]
This asymmetry is mechanical, not empirical: the parameter distribution imposed
here is symmetric by construction and the exponential map is convex, so an
asymmetric outcome distribution follows from a symmetric parameter distribution
without any asymmetry in the underlying effects. It is reported so that a reader
encountering skewed outcome ranges elsewhere knows how little must be assumed to
produce them. It is not evidence that the effect distribution is skewed.

% ¶18 — The probability bracket, quoted once.
For a pair drawn at random from the adopting population, the probability that
its true effect is non-positive lies in $[\LedgerPLo,\,\LedgerPHi]$, where the
lower endpoint is the normal-form probability evaluated at the hardened arm and
the upper endpoint is capped at the empirical share of non-positive estimates
because the normal form at the noise-only arm returns a value exceeding that
share. That bracket is the sharpest risk statement this design supports, and it
is quoted here once.

% ¶19 — The abstention.
Sharper statements are available in the literature and are not available here.
At the signal share these data achieve the shape of the effect distribution is
not identified, and every quantity a risk-minded reader would next ask for is a
statement about shape: the probability of a loss exceeding a threshold, the
quantiles of the effect distribution, the weight of an adverse tail, the share
of adopters for whom the agreement was a mistake. Each requires locating mass in
a density this design does not recover. The bracket above is a probability of
sign, obtained from two assumptions stated in the sentence reporting it, and it
does not generalise to a probability of magnitude. This paper reports no
density-based risk statement beyond it, and readers should treat the absence as
a finding about what these data can bear rather than as an omission.

\subsection{The Evaluator's Reading}
\label{sec:prop-reading}

% ¶20 — Commitments (i)-(iii).
An evaluator holding these results should carry six commitments away from them.
The first is to condition on size before consulting any coefficient, because the
conditional and unconditional expectations diverge by more than the
unconditional one at both ends of the trade distribution. The second follows: a
large, heavily trading pair should expect a small negative central effect rather
than a large positive one, while a small, thinly integrated pair should expect a
proportional gain that is large in percentage terms precisely because its base
is small. The third is that these compose to a modest aggregate --- weighted by
the dollars actually at stake, the average agreement effect is
$\LedgerTWMeanPct$ percent, not $\LedgerMeanThetaDPct$ percent, and a programme
of agreements moves aggregate trade correspondingly less than the pair-level
headline suggests.

% ¶21 — Commitments (iv)-(v).
The fourth commitment is to the ledgered bracket and no further: for a randomly
drawn adopting pair the probability of no gain or of a loss lies in the interval
reported in Section~\ref{sec:prop-uncertainty}, and no sharper risk statement is
available at this signal share. The fifth is that every propagated range here is
conditional on a dispersion arm and an imposed distributional form, and a user
quoting one inherits both conditions whether or not they are restated. Neither
is a caveat appended to a result. Each is part of the result.

% ¶22 — Commitment (vi) and the handoff.
The sixth is the binding one. Everything above describes self-selected matches:
pairs that chose an agreement, measured against a counterfactual fitted on pairs
that did not. The design identifies nothing about what an agreement would do to
a pair that did not choose one, and Section~\ref{sec:limits-transport}
establishes that this is a property of the estimating population rather than a
caution about external validity. Any policy reading extending these numbers to
non-adopters crosses a boundary the design cannot cross with it, and that is
where this section stops. What it leaves is a question about how a literature
came to report a single number for a quantity this dispersed, which the closing
section takes up.

% =============================================================================
% TABLES
% =============================================================================

\begin{table}[htbp]
\centering
\caption{Implied trade outcomes by dispersion arm}
\label{tab:propagation}
\begin{tabular}{lrrrrr}
\toprule
Arm & SD of true effects & $q_{10}$ & $q_{50}$ & $q_{90}$ & Range \\
\midrule
Hardened drift (C) & $0.740$ & $-50.1$ & $28.2$ & $231.1$ & $6.64$ \\ % [T20: C_hardened, symmetric]
Noise-only (A)     & $1.475$ & $-81.2$ & $23.5$ & $737.2$ & $44.44$ \\ % [T20: A_noise_only, symmetric]
\bottomrule
\end{tabular}
\end{table}

% Note to Table~\ref{tab:propagation}. Percentage changes in bilateral trade
% implied by the estimated effect distribution, at both endpoints of the
% identified set for the standard deviation of true effects; no arm is
% preferred. The mean is the ledgered pair-level mean effect. Because the SHAPE
% of the effect distribution is not identified at this signal share, a form must
% be imposed to obtain quantiles: a symmetric normal with the ledgered mean and
% the arm's standard deviation, 10,000 draws under a fixed seed. This
% propagation is PARTIAL: each pair's effect is mapped onto its own trade with
% multilateral resistances, third-country reallocation, incomes and expenditures
% held fixed. Range is (1 + q90/100)/(1 + q10/100). Every figure is conditional
% on both the arm and the imposed form.

\begin{table}[htbp]
\centering
\caption{Conditional-GE trade changes at two calibrated dyads}
\label{tab:gradient-ge}
\begin{tabular}{llrrrrr}
\toprule
Calibration & Arm & Mean effect & $q_{10}$ & $q_{50}$ & $q_{90}$ & Range \\
\midrule
Smallest quintile & Hardened   & $0.8554$  & $-0.6$  & $140.7$ & $524.7$  & $6.28$ \\ % [T43: lo, LUX_ATG, Q1]
Smallest quintile & Noise-only & $0.8554$  & $-57.9$ & $147.3$ & $1533.6$ & $38.79$ \\ % [T43: hi, LUX_ATG, Q1]
Largest quintile  & Hardened   & $-0.0583$ & $-60.0$ & $-3.1$  & $152.3$  & $6.31$ \\ % [T43: lo, NLD_CRI, Q5]
Largest quintile  & Noise-only & $-0.0583$ & $-83.1$ & $-0.4$  & $564.4$  & $39.22$ \\ % [T43: hi, NLD_CRI, Q5]
\bottomrule
\end{tabular}
\end{table}

% Note to Table~\ref{tab:gradient-ge}. Percentage changes in bilateral trade from
% a conditional general-equilibrium solve of a structural gravity system on the
% terminal cross-section: incomes and expenditures held at their data values,
% prices and multilateral resistances adjusting. Elasticity of substitution
% sigma = 5, taken as the literature standard and not estimated here. Two
% calibrations, both drawn from the adopting population: the median pair of the
% smallest pre-adoption trade quintile and the median pair of the largest, on
% the partition of Section~\ref{sec:results-gradient}. Each calibration draws
% 500 effects from a symmetric normal centred on its own quintile mean, at both
% endpoints of the identified set for the dispersion of true effects. Range is
% (1 + q90/100)/(1 + q10/100). Because incomes are fixed, every entry is a change
% in bilateral trade and not a welfare quantity.

% =============================================================================
% ABSTRACT FEED
% =============================================================================
% Propagated to trade outcomes, the estimated distribution implies a central
% expectation of roughly a 28 percent increase for a pair at the centre of the
% effect distribution and roughly 9 percent for the average traded dollar, a gap
% that is a consequence of the size gradient rather than a discrepancy.
% Conditioning on pre-adoption trade, the central expectation runs from about
% +135 percent in the smallest quintile to about -5.7 percent in the largest, and
% a conditional general-equilibrium solve at calibrated dyads from each end shows
% the gradient survives equilibrium adjustment: transmission is flat across the
% size distribution, so the gradient in outcomes is a gradient in the effect
% parameter. Uncertainty is reported as a bracket of brackets: ranges are indexed
% by which admissible null model sets the dispersion of true effects and by the
% symmetric form imposed where shape is unidentified. For a randomly drawn
% adopting pair the probability of a non-positive effect lies in [0.370, 0.421],
% and no sharper risk statement is supported at the achievable signal share.

% =============================================================================
% SECTION 8 — CONCLUSION
% Drafted against meta/canonical_facts.md at commit 88d0fb70.
% ID budget: 3 IDs in body text (SD_THETA_TRUE, P_THETA_LEQ_0, GRADIENT).
% Cross-references unlimited. Structure per SECTION8-2: P1 takes up the
% question handed over at the close of sec:prop-reading; P2 is the prediction
% ledger promised at lines 117 and 405; P3 is method, prospective; P4 is the
% disclaimer; P5 closes.
% =============================================================================

\section{Conclusion}
\label{sec:conclusion}

% P1a — Take up Section 7's handoff: how one number survived this long.
Section~\ref{sec:propagation} closes on a question rather than a result: how a
literature came to report a single number for a quantity this dispersed. The
answer is not inattention. It is that the two things a researcher would have
needed in order to know better were unavailable in opposite directions, and
neither absence announced itself.

% P1b — The two facts, and what sits between them.
The diagnostics that could be computed were confidently wrong. Estimating
effects pair by pair and asking whether they replicate returns reliability high
enough to clear any conventional threshold, and
Section~\ref{sec:empirical-certificate} shows that the same machinery returns
much of it on a population constructed to have no effects to be reliable about.
Persistence was measured and validity inferred; Corollary~\ref{cor:rel} states
the condition under which that inference fails, and the condition is met by
ordinary features of trade data rather than by anything exotic. The quantities
most wanted, meanwhile, are not merely hard to compute but beyond computation at
any sample size: Proposition~\ref{prop:ident}(b) shows that transitory variation
in the effect and a treatment-coincident change in volatility reach the
likelihood only through their sum, so no statistic separates them and no larger
panel will (Section~\ref{sec:limits-equivalence}). A field that could not verify
its pair-level estimates, and could not in principle resolve the ambiguity
beneath them, had reason to report the average and move on. Between those two
facts sits what this paper claims. The dispersion of true effects is an
identified set, $[\LedgerSDTrueLo,\,\LedgerSDTrueHi]$, whose endpoints are
% [SD_THETA_TRUE]
indexed by a noise-attribution assumption the data do not adjudicate. The
probability that a randomly drawn adopting pair's effect is non-positive is a
bracket, $[\LedgerPLo,\,\LedgerPHi]$, and no sharper risk statement is
% [P_THETA_LEQ_0]
available at this signal share. The gradient in pair size spans
$\LedgerGradient$ log points and is sharp enough to carry a policy reading on
% [GRADIENT]
its own (Section~\ref{sec:results-gradient}). And the specification spread the
discipline has treated as fragility is resolved rather than adjudicated: the
competing estimates are differently weighted averages of one sloped
distribution (Section~\ref{sec:results-spread}).

% P2 — The prediction ledger promised at lines 117 and 405. Two carry weight,
% and not equally; one under-identified, one untested. Per sec:results-reconciliation.
Four predictions were advanced, and Section~\ref{sec:theory-standard} asked that
they be held jointly because each is weak alone. Two carry weight. Effects
graded in pair size and prior integration are confirmed, in the fall-then-floor
form the mechanism requires rather than as a bare negative slope
(Section~\ref{sec:results-gradient}). Selection on match quality is supported,
not confirmed, by the placebo and stability evidence of
Section~\ref{sec:empirical-diagnostics}. Two do not. Right-skewed effects are
neither confirmed nor refuted: the shape of the effect distribution is not
identified at the signal share these data achieve, which is a limit of the
measurement and not a null (Section~\ref{sec:results-reconciliation}).
Lognormal-type outcome tails are untested here, the prediction concerning the
marginal distribution of flows and this design reaching it only through a
normality assumption Section~\ref{sec:limits-tolerances} reports as rejected.
The two that carry are not worth the same. The gradient is the prediction
Section~\ref{sec:theory-nonsep} named as the sharpest of the four, because it
specifies a shape as well as a direction and nominates its own falsifier in a
smoothly monotone descent; the quintile means were unrestricted, the falsifier
was available, and it did not appear. Selection corroborates without separating,
being consistent with almost any account of why governments negotiate. Recording
the open legs alongside the closed ones is the standard the theory section set
for itself, and it is the standard on which this paper asks to be read.

% P3 — Exportable method, prospective. No account of how the paper came to need it.
The instruments this required are not specific to trade agreements. An effect
distribution estimated from panel data is certified, if at all, by instruments
built to be adversarial to it. A placebo population with nothing to recover, run
through the identical pipeline, converts a reliability statistic from a claim
about the estimates into a claim about the machinery that produced them. A
pair's own pre-adoption years supply a null calibrated on the unit itself rather
than on a pooled reference, which is what makes drift separable from effect.
Planted truth of known magnitude, recovered end to end, tests the pipeline and
not merely the estimator at its final step. Identified sets reported with their
arms labelled keep an assumption visible at the point where a single number
would launder it into a result. And provenance enforced by machine --- every
stated quantity bound to a committed artifact, and the binding checked rather
than asserted --- makes a manuscript auditable against its own outputs.
Section~\ref{sec:empirical-diagnostics} states these as five practices; taken
together they are one standard, which is that a number reported about an effect
is reported alongside what the same procedure returns when there is no effect to
find.

% P4 — What the paper does not conclude. Only site at which the introduction's
% floor/downside/upside triple may be named, and only as failed objects.
Two things this paper does not conclude deserve stating as plainly as what it
does. Nothing here evaluates an agreement assigned to a pair that did not choose
one: the estimand is defined over self-selected matches, and
Section~\ref{sec:limits-transport} establishes that as a property of the
estimating population rather than a caution about external validity. And nothing
here supplies the quantities a decision-maker would most want --- a floor, a
sharp probability of being made worse off, a realistic upside --- because each is
a property of a shape this design cannot identify; what is available is the
bracket above. The paper equips the question. It does not answer it for anyone.

% P5 — Close. One callback to the introduction's promise; nothing after.
The single number was never wrong so much as it was one summary of an object
that does not reduce to a summary. What stands in its place is a set, a bracket
and a gradient: less than the introduction promised, and more than was
previously known. Heterogeneity in the effects of trade agreements is not a
complication surrounding the answer. It is the quantity the question was always
about.

\newpage
\bibliographystyle{aer}
\bibliography{references}

\newpage
\appendix

\section{Additional Tables and Figures}
\label{app:tables}

% =============================================================================
% APPENDIX TABLE — DRIFT-CALIBRATION HOLDOUT (T9)
% Body: output/T9_placebo_holdout.csv (producer code/S5R_bhat_split.R).
% Row NA is the pooled validation set; rows 1-10 are size deciles.
% Ledger rows: HOLDOUT_N, HOLDOUT_MEAN, HOLDOUT_D3_MEAN, HOLDOUT_D3_N,
% HOLDOUT_D3_SE, HOLDOUT_D1_N, HOLDOUT_D2_N, HOLDOUT_D8_N,
% HOLDOUT_MAX_ABS_EX_D3.
% =============================================================================

\begin{table}[htbp]
\centering
\caption{Held-out validation of the drift correction}
\label{tab:holdout}
\begin{tabular}{lccc}
\toprule
Size decile & Validation pairs & Corrected placebo effect & SE \\
\midrule
Pooled & $8{,}545$ & $0.0188$ & $0.0087$ \\ % [HOLDOUT_N, HOLDOUT_MEAN]
\midrule
1  & $19$    & $0.0464$  & $0.1525$ \\ % [HOLDOUT_D1_N]
2  & $94$    & $0.0532$  & $0.0928$ \\ % [HOLDOUT_D2_N]
3  & $\HoldoutDThreeN$   & $\HoldoutDThreeMean$ & $0.0591$ \\ % [HOLDOUT_D3_N, HOLDOUT_D3_MEAN, HOLDOUT_D3_SE]
4  & $630$   & $0.0314$  & $0.0362$ \\ % [T9: decile 4]
5  & $1{,}019$ & $0.0569$  & $0.0302$ \\ % [HOLDOUT_MAX_ABS_EX_D3]
6  & $1{,}238$ & $0.0299$  & $0.0266$ \\ % [T9: decile 6]
7  & $1{,}343$ & $0.0514$  & $0.0228$ \\ % [T9: decile 7]
8  & $1{,}365$ & $0.0078$  & $0.0214$ \\ % [HOLDOUT_D8_N]
9  & $1{,}327$ & $-0.0082$ & $0.0166$ \\ % [T9: decile 9]
10 & $1{,}196$ & $0.0023$  & $0.0148$ \\ % [T9: decile 10]
\bottomrule
\end{tabular}

\vspace{0.5em}
\begin{minipage}{0.95\textwidth}
\footnotesize
\emph{Notes.} The drift correction of Definition~$D$ subtracts a size-cell
placebo mean. This table applies the correction out of sample: cell means are
estimated on one split of the placebo population and applied to a validation
split the estimation never saw. If the correction were fitting noise, the
corrected effect on the validation pairs would depart from zero, which is the
value the placebo population's construction guarantees. Pooled, it does not.
Decile~3 is the one cell whose mean is more than one standard error from zero,
and it is also the cell whose sign is negative; excluding it, the largest
absolute decile mean is $\HoldoutMaxAbsExDThree$. The thinnest folds carry
standard errors an order of magnitude above the pooled figure, so the
per-decile columns discipline the pooled result rather than standing on their
own.
\end{minipage}
\end{table}

% =============================================================================
% APPENDIX TABLE — SPLIT-HALF RELIABILITY (T22)
% Body: output/T22_reliability.csv (producer code/S24_reliability.R).
% Ledger rows: SPLITHALF_A_R, SPLITHALF_A_RELIABILITY, THETA_A_MEAN,
% THETA_A_SD, PLACEBO_A_R, PLACEBO_A_RELIABILITY, PLACEBO_A_MEAN,
% PLACEBO_A_SD, PLACEBO_A_N, TREATED_TH, PLACEBO_TH, TREATED_TPOST,
% PLACEBO_TPOST.
% Population differs from tab:correction: the >=2-post-cells-per-half rule.
% =============================================================================

\begin{table}[htbp]
\centering
\caption{Split-half reliability under Definition~$A$, treated and placebo}
\label{tab:reliability}
\begin{tabular}{lcc}
\toprule
 & Switching pairs & Zero-effect benchmark \\
\midrule
Mean $\theta_A$                  & $-0.2548$ & $\LedgerPlaceboAMean$ \\ % [THETA_A_MEAN, PLACEBO_A_MEAN]
SD $\theta_A$                    & $1.6318$  & $\LedgerPlaceboASD$ \\ % [THETA_A_SD, PLACEBO_A_SD]
\addlinespace
Split-half Pearson $r$           & $\LedgerSplithalfR$ & $\LedgerPlaceboR$ \\ % [SPLITHALF_A_R, PLACEBO_A_R]
Spearman--Brown reliability      & $0.9607$  & $0.8547$ \\ % [SPLITHALF_A_RELIABILITY, PLACEBO_A_RELIABILITY]
\addlinespace
Mean post cells per split half   & $5.88$    & $5.46$ \\ % [TREATED_TH, PLACEBO_TH]
Mean post cells per pair         & $11.76$   & $10.91$ \\ % [TREATED_TPOST, PLACEBO_TPOST]
\addlinespace
$n$                              & $4{,}120$ & $\LedgerPlaceboN$ \\ % [PLACEBO_A_N]
\bottomrule
\end{tabular}

\vspace{0.5em}
\begin{minipage}{0.95\textwidth}
\footnotesize
\emph{Notes.} Both columns are computed on the subset of pairs with at least
two post-adoption cells in each split half, which is why the row denominators
differ from those of Table~\ref{tab:correction}. The placebo column is the
same estimator applied to never-treated pairs carrying pseudo-adoption dates,
whose true effect is zero by construction. The comparison is the point of the
table: a reliability coefficient of $\LedgerSplithalfR$ on the treated pairs
is not evidence that the estimates recover effects, because the identical
procedure returns $\LedgerPlaceboR$ where there are no effects to recover.
Corollary~\ref{cor:rel} gives the condition under which this holds, and the
post-cell rows report the window lengths that enter it.
\end{minipage}
\end{table}

\section{Proofs}
\label{app:proofs}

\section{Propositions}

\subsection*{Notation and assumptions}
$y_{ijt}$ observed trade; $\hat y_{ijt}$ the PPML-imputed counterfactual;
$\eta_{ijt}:=y_{ijt}/\hat y_{ijt}$; $g_{ijt}:=\log\eta_{ijt}$.
Definitions: $\theta^A_{ij}:=T^{-1}\sum_{t\in\text{post}}g_{ijt}$;
$\theta^B_{ij}:=\log\!\big(\sum_{t}y_{ijt}/\sum_t\hat y_{ijt}\big)$ over the post window.

\begin{assumption}[A1] $y_{ijt}=\hat y_{ijt}\eta_{ijt}$ with $\E[\eta_{ijt}]=1$.\end{assumption}
\begin{assumption}[A2] $\log\eta_{ijt}\sim N(-\sigma^2_{ij}/2,\sigma^2_{ij})$, i.i.d.\ over $t$ within pair.\end{assumption}
\begin{assumption}[A3] $\sigma^2_{ij}$ varies across pairs and is a persistent pair trait.\end{assumption}

%------------------------------------------------------------
\begin{proposition}[Jensen bias and its reliability]\label{prop:jensen}
Under (A1)--(A2) with zero treatment effect:
\emph{(a)} $\E[\theta^A_{ij}\mid ij]=-\sigma^2_{ij}/2$ exactly.
\emph{(b)} Write $R_{ij}=\sum_t w_t\eta_{ijt}$ with $w_t=\hat y_{ijt}/\sum_s\hat y_{ijs}$. Then
$\E[\theta^B_{ij}]=-\tfrac12\Var(R_{ij})+r_{ij}$, where
$\Var(R_{ij})=\Var(\eta_{ij})\sum_t w_t^2$ (equal weights: $\Var(\eta_{ij})/T$),
$\Var(\eta_{ij})=e^{\sigma^2_{ij}}-1$, and the remainder $r_{ij}$ collects terms of
order $\E[(R-1)^3]$ and higher; the expansion requires $\Var(R_{ij})\ll1$.
\emph{(c)} For split-half estimates on disjoint post halves of length $T_h$,
\[
\mathrm{Corr}\big(\theta^{A,(1)},\theta^{A,(2)}\big)
=\frac{\Var_{\text{pairs}}(\sigma^2_{ij})/4}
{\Var_{\text{pairs}}(\sigma^2_{ij})/4+\E[\sigma^2_{ij}]/T_h},
\]
exactly under (A2).
\end{proposition}

\begin{proof}
(a) Immediate from (A2). (b) Second-order expansion of $\log R$ about $R=1$ with
$\E[R]=1$; the weight identity and $\Var(\eta)=e^{\sigma^2}-1$ (from $\E[\eta^2]=e^{\sigma^2}$
under (A2)) give the display. (c) Conditional on the pair,
$\theta^{A,(h)}=-\sigma^2_{ij}/2+\bar\varepsilon^{(h)}$ with
$\bar\varepsilon^{(h)}\sim(0,\sigma^2_{ij}/T_h)$ independent across halves; decompose
variance and covariance across pairs.
\end{proof}

\begin{corollary}[Reliability paradox]\label{cor:rel}
Let $b_{ij}$ be \emph{any} persistent per-pair bias with cross-pair variance
$\Var(b)$; then under zero effects
$r_{\mathrm{split}}=\Var(b)/\big(\Var(b)+\E[\sigma^2]/T_h\big)$.
High split-half reliability certifies persistence, not validity: any dispersed,
persistent artifact (Jensen, drift) passes.
\end{corollary}

% REMARK 1: Empirical scale (uses prop_constants.tex macros) - D5.1 rewrite
% LEDGER: PLACEBO_A_MEAN=-0.6821 (\PropPlaceboAMean), PLACEBO_B_UNCORR_MEAN=-0.2072 (\PropPlaceboBUncorr)
\begin{remark}[Empirical scale; validity of the expansions]
Part~(a) identifies the mean dispersion directly from the zero-effect
population, since $\E[\theta^A]=-\E[\sigma^2]/2$ exactly: at the R-chain placebo
mean $\PropPlaceboAMean$ under Definition~$A$ this gives
$\E[\sigma^2]=\PropEsigsq$ and $\sigma=\PropSigma$. At that dispersion
$\Var(\eta)=\PropVarEta$, so the generic Taylor form $-\Var(\eta)/2$ is
\emph{invalid} for $\theta^A$ and the exact lognormal value applies; and for
$\theta^B$ at $T=10$, $\Var(R)=\PropVarR$, so $-\Var(R)/2=\PropApproxB$ is a
rough guide only. Simulation at the same $(\sigma,T)$ gives
$\E[\theta^B]=\PropMCB$: the second-order expansion overstates the bias
magnitude by about $\PropOverstatePct$ percent, the positive third-order
(skewness) term partially offsetting the variance term, so the approximation is
conservative at this scale. The R-chain $B$-placebo mean $\PropPlaceboBUncorr$
% [T24: PLACEBO_B_UNCORR_MEAN]
additionally contains drift (Proposition~\ref{prop:drift}).
Consistency check [V1c]: part~(c) is over-identified here, and the check is
parameter-free. The cross-pair variance of $\theta^A$ on the zero-effect
population decomposes as $\Var(\sigma^2)/4+\E[\sigma^2_{ij}/T^{\mathrm{post}}_{ij}]$,
and the second term must be evaluated pair by pair rather than at the mean
window length: post windows differ across pairs, $1/T$ is convex, and the
reciprocal of the mean understates the mean of the reciprocals. Evaluated
pair-wise, the decomposition yields $\Var(\sigma^2)=\PropVsigmasq$, a
coefficient of variation of $\sigma^2_{ij}$ of $\PropCVsigsq$. Substituting
that into the split-half formula, again pair-wise, \emph{predicts} a placebo
reliability of $\PropRpred$ against the observed $\PropPlaceboR$
% [PLACEBO_A_R]
--- a discrepancy of $\PropRgap$, over-predicting slightly, with no parameter
fitted to it. Evaluating the same decomposition at the mean window length
instead returns $\Var(\sigma^2)=\PropVsigmasqPlugin$ and a reliability whose
error is roughly eight times larger, in the direction convexity requires. The
pair-wise evaluation is therefore not a refinement of the proposition but the
correct reading of it.
\end{remark}

%------------------------------------------------------------
\begin{proposition}[Within-pair drift]\label{prop:drift}
Relax (A2) to $\log\eta_{ijt}=-\sigma^2_{ij}/2+\delta_{ij}\,(t-\bar t_{ij})+u_{ijt}$,
$u_{ijt}\stackrel{iid}{\sim}N(0,\sigma^2_{ij})$, with $\delta_{ij}$ a persistent
pair-specific drift and $\bar t_{ij}$ the pair's sample midpoint (the pair fixed
effect centers the level, not the trend). For a never-treated pair with span
$T=T_{\mathrm{pre}}+T_{\mathrm{post}}$ and post window equal to the last
$T_{\mathrm{post}}$ years:
\emph{(a)} $\overline{(t-\bar t)}^{\,\mathrm{post}}=T_{\mathrm{pre}}/2$, hence to
first order
$\E[\theta^{A}_{ij}]=-\sigma^2_{ij}/2+\delta_{ij}T_{\mathrm{pre}}/2$
(and $\theta^B$ inherits the same drift term with $\hat y$-weighted window mean);
the expression depends on window geometry only, never on calendar time.
\emph{(b)} The drift term is a persistent pair trait and passes split-half
reliability by Corollary~\ref{cor:rel}.
\emph{(c)} Now let true effects $\theta_{ij}\sim(\mu,\tau^2)$ enter the post
window. If $\delta_{ij}$ correlates with pair size $s$, the Definition-$D$
correction (subtracting the size-cell placebo mean) removes
$\E[\delta_{ij}\mid s]\,T_{\mathrm{pre}}/2$ but not the within-cell dispersion:
\[
\Var(\theta^D\mid s)\;=\;\tau^2+\big(T_{\mathrm{pre}}/2\big)^2\Var(\delta_{ij}\mid s)
+\frac{\E[\sigma^2_{ij}\mid s]}{T_{\mathrm{post}}},
\]
so any deconvolution recovers $\tau^2$ jointly with the within-cell drift
variance unless the null design carries matching window geometry.
\end{proposition}

\begin{proof}
(a) The post-window mean of $t$ is $T_{\mathrm{pre}}+(T_{\mathrm{post}}+1)/2$;
subtracting $\bar t=(T+1)/2$ gives $T_{\mathrm{pre}}/2$. Calendar year enters
nowhere. (b) Apply Corollary~\ref{cor:rel} with
$b_{ij}=-\sigma^2_{ij}/2+\delta_{ij}T_{\mathrm{pre}}/2$. (c) Decompose
$\theta^D=\theta_{ij}+\big(\delta_{ij}-\E[\delta\mid s]\big)T_{\mathrm{pre}}/2+
\bar u$, take variances conditional on $s$.
\end{proof}

% REMARK 2: Window geometry (Prop 2a verification) - D2 rewrite
% LEDGER: V2 predicted = \PropVtwoPred with delta=0.02
\begin{remark}
Part~(a) predicts a placebo bias governed by window geometry alone and, where
$\delta_{ij}$ correlates with size, graded in size. Simulation confirms both:
at $\delta=0.02$ the mean pseudo-effect matches the predicted $\PropVtwoPred$ at
$\PropVtwoSim$; shifting every pair's span by an arbitrary calendar offset
leaves the result unchanged (shift $\approx\PropVtwoShift$, confirming calendar invariance).
% [T25: PROP_V2_PRED, PROP_V2_SIM, PROP_V2_SHIFT]
 Empirically the R-chain placebo means under Definition~$B$ are graded over the
upper size deciles, falling in magnitude from $-0.365$ at the fifth decile to
% [T24: PLACEBO_B_UNCORR_DECILE5, PLACEBO_B_UNCORR_DECILE10]
$-0.057$ at the largest, and are non-monotone below the fifth: the smallest
decile returns $-0.206$ against an overall mean of $-0.207$.
% [T24: PLACEBO_B_UNCORR_DECILE5, DECILE10, DECILE1, OVERALL]
 The proposition predicts grading conditional on the size--drift correlation and
does not predict monotonicity across the full support; the observed profile is
reported as it stands.
\end{remark}

%------------------------------------------------------------
\begin{proposition}[Identification and its boundary]\label{prop:ident}
Let pre-window gaps be $g_{ijt}=\sigma_{ij}u_{ijt}$ (mean-adjusted) and
post-window gaps
$g_{ijt}=\theta_{ij}+\nu_{ijt}+\sigma_{ij}\rho\,u_{ijt}$, with
$u_{ijt}\stackrel{iid}{\sim}N(0,1)$, transitory effect variation
$\nu_{ijt}\stackrel{iid}{\sim}N(0,\omega^2)$ independent of $u$, persistent
effects $\theta_{ij}\sim(\mu,\tau^2)$, and post/pre volatility ratio $\rho\ge0$.
Define the per-pair observables: pre variance $V^{\mathrm{pre}}_{ij}$, centered
post variance $V^{\mathrm{post}}_{ij}$, and $\hat\theta_{ij}=\bar g^{\mathrm{post}}$.
Then:
\emph{(a) (Identification of $\tau^2$.)}
$\E[V^{\mathrm{post}}_{ij}]=\sigma^2_{ij}\rho^2+\omega^2$,
$\Var(\hat\theta_{ij}\mid ij)=V^{\mathrm{post}}_{ij}/T_{\mathrm{post}}$ exactly, hence
\[
\tau^2=\Var_{\text{pairs}}(\hat\theta)-\E\big[V^{\mathrm{post}}\big]/T_{\mathrm{post}},
\]
with all right-hand quantities estimable (the within-pair sample variance is
unbiased for $V^{\mathrm{post}}$).
\emph{(b) (Exact non-identification of $(\omega^2,\rho)$.)} Under the stated
normality, the post gaps are, conditionally on the pair, i.i.d.\
$N(\theta_{ij},\,\sigma^2_{ij}\rho^2+\omega^2)$: the parameters enter the
likelihood only through the composite $\sigma^2\rho^2+\omega^2$. Any two
parameterizations with equal composites generate \emph{identical} data
distributions; the split between transitory effect heterogeneity and
treatment-coincident volatility change is a modeling label, not an estimable
quantity. The equivalence is exact under the stated normality, and is claimed at
that strength and no further. Absent normality the composite no longer exhausts
the likelihood, and separation becomes possible in principle through within-pair
moments of order $\ge3$; the sampling noise of any \emph{per-pair} such moment
overwhelms it at $T_{\mathrm{post}}\approx10$, but the operative statistic pools
across pairs and its noise falls with the number of pairs, not the window length.
Remark~\ref{rem:moments} reports what that pooled route delivers at this sample.
\end{proposition}

\begin{corollary}[Operational variance ratios over-subtract]\label{cor:rhoop}
Define $\hat\rho^2_{\mathrm{op}}:=V^{\mathrm{post}}/V^{\mathrm{pre}}
=\rho^2+\omega^2/\sigma^2$. A ``post-calibrated'' null that scales a
\emph{pre-based} noise variance of window length $T_0<T_{\mathrm{post}}$ by
$\hat\rho^2_{\mathrm{op}}$ subtracts
$\kappa_w\,V^{\mathrm{post}}/T_{\mathrm{post}}$ with window factor
$\kappa_w:=T_{\mathrm{post}}/T_0>1$, biasing the recovered variance by
$-(\kappa_w-1)\,\E[V^{\mathrm{post}}]/T_{\mathrm{post}}$. At
$\kappa_{\mathrm{floor}}$ the entire treatment variance is absorbed:
$\hat\tau^2(\kappa_{\mathrm{floor}})=0$.
\end{corollary}

% REMARK 3: Empirical bracket and cor:rhoop (V3c verification) - D1 rewrite
% LEDGER: SD_THETA_TRUE=[0.74,1.48] (INV-027); kappa_floor = \PropKappaFloor
\begin{remark}[The empirical bracket, organized by arm]
Part~(a) is precisely the window-matched estimator used in the R-chain V1
computation. The $\mathrm{SD}_{\mathrm{true}}$ bracket $[0.74,1.48]$ (INV-027)
is arm-indexed:
Arm~C (OOS drift subtraction, $\mathrm{Var}_{\mathrm{null}}=1.89$) yields $\mathrm{SD}_{\mathrm{true}}=0.74$;
Arm~A (noise-only subtraction, $\mathrm{Var}_{\mathrm{null}}=0.26$) yields $\mathrm{SD}_{\mathrm{true}}=1.48$.
Under the stated model, the interval spans the admissible noise attributions.
Corollary~\ref{cor:rhoop} quantifies what a mis-specified null window costs. At
window factor $\kappa=1$ the subtraction is the noise-only arm and returns its
$\mathrm{SD}_{\mathrm{true}}$ of $\PropTauKone$ by construction; at
$\kappa=2,3,5$ the recovered dispersion falls to $\PropTauKtwo$, $\PropTauKthree$
and $\PropTauKfive$, reaching zero at $\kappa_{\mathrm{floor}}=\PropKappaFloor$.
% [T25: Cor:rhoop rows]
 A null calibrated on a window shorter than the estimation window therefore
absorbs true heterogeneity at a computable rate, and at
$\kappa_{\mathrm{floor}}$ absorbs all of it.
The transitory slice $\omega^2/T$ is, by part~(b), exactly inseparable from
volatility change under normality; Remark~\ref{rem:moments} bounds what
departures from normality recover.
\end{remark}

% REMARK 4: the pooled higher-moment route (S30 power calculation + S34 measurement)
% LEDGER: MOMPOW_IDENT_* (INV-040 corrected definition); A2_EXKURT, A2_EXKURT_CONTROL
\begin{remark}[What departures from normality recover]\label{rem:moments}
Part~(b)'s equivalence is exact under normality and silent outside it. The
question outside it is quantitative: pooled across pairs, does a higher-order
within-pair moment separate the two parameterizations at this sample? A
calibrated simulation answers it. Drawing $\sigma^2_{ij}$ to match the ledgered
first two moments and using the empirical distribution of post-window lengths,
the two worlds are constructed with equal composites and compared on the pooled
mean of per-pair sample excess kurtosis. Under normality the difference is zero
to simulation precision, as part~(b) requires.
% [MOMPOW_KAPPA0_DELTA]
 Away from it, and expressed as the ratio of the difference to the sampling
standard deviation of the statistic in a \emph{single} realization of $4{,}182$
pairs, separation runs from $\MomPowMinZ$ to $\MomPowMaxZ$ across excess kurtosis in
$[0.75,\,3.0]$ and transitory shares in $[0.10,\,1.00]$.
% [MOMPOW_IDENT_MIN_Z, MOMPOW_IDENT_MAX_Z]
 The relevant departure is measured rather than assumed: the standardized log
gaps carry an excess kurtosis of $\AtwoExKurt$ against $\AtwoExKurtControl$ for a matched normal
control passed through identical code, a net departure near unity that falls
inside the simulated grid.
% [A2_EXKURT, A2_EXKURT_CONTROL]


Two qualifications bound the reading, and both are material. The simulation
treats pairs as independent; cross-pair dependence is not estimated anywhere in
this paper, and at assumed variance inflations of ten and fifty the same
separation falls to $3.62$ and $1.62$ at the most favorable cell.
% [MOMPOW_IDENT_DEFF10_MAX_Z, MOMPOW_IDENT_DEFF50_MAX_Z]
 And the route requires committing to a shape for the multiplicative shock that
treatment does not alter; if treatment may change the shape as well as the scale,
the separation closes again. The honest statement is therefore conditional. The
split between transitory effect heterogeneity and treatment-coincident volatility
change is exactly unidentified under normality; outside normality it is
recoverable at this sample under cross-pair independence and shape invariance,
and its status without those two conditions is not established here. Nothing in
this paper's reported interval depends on which way that resolves, since the
interval concerns the persistent component identified by part~(a).
\end{remark}

\end{document}
