FILE:           a2_constants.tex
SHA256:         a307048d358496978254220ab867f5ae71d96413029a80b5979c0b5d1b23bd3d
PRODUCER:       code/S42_a2_constants.R
PRODUCER_SHA256: e9b399fadce779fb8cb7798ac738b7ff12ac98ef418347724501036bd8f480a2
INPUTS:
  meta/canonical_facts.md: (runtime read, ledger)
SEED:           NONE

SPECIFICATION:
  Section 6 and Appendix A.2 constants.
  Seven macros parsed from canonical_facts.md ledger at runtime:

  Appendix A.2 (Remark 4):
    \MomPowMinZ           <- MOMPOW_IDENT_MIN_Z (precision 2)
    \MomPowMaxZ           <- MOMPOW_IDENT_MAX_Z (precision 2)
    \AtwoExKurt           <- A2_EXKURT (precision 3)
    \AtwoExKurtControl    <- A2_EXKURT_CONTROL (precision 3)

  Section 6 (drift-calibration holdout):
    \HoldoutDThreeMean    <- HOLDOUT_D3_MEAN (precision 4)
    \HoldoutDThreeN       <- HOLDOUT_D3_N (integer, no decimal)
    \HoldoutMaxAbsExDThree <- HOLDOUT_MAX_ABS_EX_D3 (precision 4)

GATES:
  G1: All seven ledger IDs found: PASS
  G2: Round-trip verification (reparsed == emitted): PASS
  G3: Exactly seven macros defined: PASS

CREATED: 2026-08-03
